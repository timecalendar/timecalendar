{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^5.4.3",
    "husky": "^7.0.0"
  },
  "scripts": {
    "prepare": "husky install",
    "postinstall": "lerna bootstrap --no-ci",
    "test": "lerna run test"
  }
}
