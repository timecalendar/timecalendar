services:
  nginx:
    image: nginx
    restart: always
    ports:
      - '1443:443'
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
      - ../ci/certificates:/etc/nginx/certificates
  postgres:
    image: postgres:14
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_HOST_AUTH_METHOD: trust
    ports:
      - '37291:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./bin/init-postgres.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d timecalendar"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
  redis:
    image: redis
    restart: always
    ports:
      - '37292:6379'
    volumes: 
      - redis_data:/data
    healthcheck:
      test: ["CMD-SHELL", "redis-cli ping | grep -q PONG"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
    driver: local
  redis_data:
    driver: local
