# (HUMAN: Activity screen real-device pass)

## What I need

Run Activity on physical iOS and Android hardware and record the result here, then remove or move
this note to `done/`.

## Why

This host has no emulator or simulator. Automation proves localized states, flattened list shape,
semantic roles, live-region props, wrapping posture, token use, and declared touch sizes, but it
cannot judge real assistive-technology speech, panel colors, or low-end scrolling.

## How to verify

- With VoiceOver and TalkBack, confirm heading navigation, top-to-bottom focus order, the single
  Settings-row announcement with its real unread count, item labels, and polite error announcements.
- At the largest Dynamic Type scale, confirm long calendar names, titles, locations, changed-field
  lines, and the Settings badge wrap or shrink without clipping.
- In both schemes, confirm positive and informational labels remain legible and distinct.
- On a representative low-end Android device, smoothly scroll a group with hundreds of children.

## Blocks

Nothing — informational device-only Definition-of-Done follow-up; it does not block PR #330.
