# Phase 02 — Pattern establishment

> **Goal:** build three deliberately-varied features that each stress a *different* architectural axis, then **extract and harden the golden-path template** the rest of the app copies. This is where the pattern is *earned*, not declared.
>
> **Depends on:** Phase 01. **Modules:** `settings`, `personal_event` (partial), `school` (read path).
>
> This is Phase 1 + 1.5 in [`migration-approach.md`](../00-exploration/migration-approach.md) §4. Order fixed by [K-4](../00-exploration/migration-approach.md#8-resolved-knobs-phase-0-kickoff-decisions).

## Rough steps

1. **Feature A — Settings.** MMKV-backed prefs (replaces `pref` + `shared_preferences`), native controls (Expo UI switches/pickers), full i18n surface, theme/dark-mode. Axis: **local KV + native controls + i18n**. — **A1 data layer landed** (`add-mobile-settings-prefs` / TIM-130, 2026-06-14): the first feature folder, the typed theme + language prefs behind `@/storage`, the reactive hooks, the C1 theme-override + i18n startup/runtime wiring, and the **K-3 coverage gate now enforced** (ADR 003). **A2 screen + native controls landed** (`add-mobile-settings-screen` / TIM-131, 2026-06-14): the presentational Settings screen + thin route, the two native `@expo/ui` pickers (theme, language) driving A1's hooks via the **first `@expo/ui` chrome wrapper** (`src/components/chrome/expo-ui.tsx`, discharging theming-D6), the Profile→Settings reachability (Stack sibling + accessible entry control + dev deep link), FR/EN strings, the proof test, and the Maestro flow — all automatable DoD axes green (ADR [010](../../../.claude/rules/mobile/decisions/010-expo-ui-chrome-wrapper.md)). **A2 is the first interactive product control through the full DoD**; **Feature A's full DoD pass is pending the inboxed on-device axes** (VoiceOver/TalkBack, native-picker feel, contrast, touch-by-finger, jank, Maestro-through-the-picker — `docs/react-native-migration/inbox/2026-06-14-settings-screen-dod-manual.md`).
2. **Feature B — Personal events (CRUD only, no calendar overlay yet).** Drizzle/SQLite table, create/edit/delete, forms, native date/time pickers, list rendering. Axis: **structured device-local data + forms + offline writes.** *(Design the schema so the [Phase 09](./09-data-migration.md) importer can target it.)* — **B1 data layer landed** (`add-mobile-personal-events-data` / TIM-132, 2026-06-14): the repo's first real `src/db/schema.ts` (`personalEvents`, importer-targetable columns), the first real migration (replacing the empty bundle), the `@/db` seam widened (`eq`/`and`/`gte`/`lte`/`useLiveQuery` + table), the `personal-events/data/` repository CRUD + range query + row↔domain mappers + uid wrapper (`expo-crypto`) + reactive `usePersonalEvents` hook, all under the K-3 gate (ADR [011](../../../.claude/rules/mobile/decisions/011-personal-event-storage.md)). **B2 UI layer landed** (`add-mobile-personal-events-ui` / TIM-133, 2026-06-14): the reactive Home-tab list over B1's `usePersonalEvents`, the single create/edit/delete form route (optional `uid` → prefill + delete, Stack sibling + dev deep link), the form/validation **logic** in `src/features/personal-events/form/` (90%-gated `validateEventForm`/`buildEventFromForm`/save-delete-edit hooks) vs. the presentational screens (70% floor), the native date/time pickers via the **second `@expo/ui` chrome consumer** (`DateTimePicker`, ADR [012](../../../.claude/rules/mobile/decisions/012-personal-event-datetime-picker.md) — `@expo/ui`'s own control, no new dep), a preset color-swatch picker storing `#RRGGBB` verbatim, FR/EN strings, a real **write error path** through `@/firebase` (the first feature where a write can fail), the proof tests, and a Maestro CRUD round-trip flow — all automatable DoD axes green. **B2 is the first feature with a multi-field form + a real write error path.** **Feature B's full DoD pass is pending the inboxed on-device axes** (VoiceOver/TalkBack, native-picker feel, contrast, touch-by-finger, jank, Crashlytics arrival, Maestro-through-the-native-picker — `docs/react-native-migration/inbox/2026-06-14-personal-events-ui-dod-manual.md`).
3. **Feature C — School selection (read path).** TanStack Query server read, offline cache via persister, nested navigation. Axis: **server state + offline read + navigation.** — **Landed** (`add-mobile-school-selection` / TIM-134, 2026-06-14): the **first real server-data read flow** — the earned query policy (`staleTime`/`gcTime ≥ maxAge`/`retry`) on the extracted `src/api/query-client.ts`, the **offline cache via a sync TanStack Query persister backed by the `@/storage` MMKV seam** (the new `mmkvQueryStorage` adapter + `PersistQueryClientProvider`, ADR [013](../../../.claude/rules/mobile/decisions/013-query-persister-and-policy.md)), the `src/features/school-selection/` `data/` (query layer over the generated hooks) + `store/` (typed, validated, identity-only selection store, derived onboarding-complete), the **nested onboarding route group** (`src/app/onboarding/` thin entrypoints over `src/components/onboarding/` screens, a Stack sibling of `(tabs)`, a Profile entry link), the **retirement of the `schools` harness surface** (replaced by `.maestro/onboarding.yaml` as the live `GET /schools` round-trip proof), FR/EN `onboarding.*` strings, the CI proofs (90% data/store, 70% screens, K-3 gate unchanged) — all automatable DoD axes green. **Feature C's full DoD pass is pending the inboxed on-device axes** (offline behavior, nested-nav feel, VoiceOver/TalkBack, contrast, touch-by-finger, jank, Maestro run — `docs/react-native-migration/inbox/2026-06-14-school-selection-dod-manual.md`). **All three pattern-establishment features have now landed their automatable DoD** → Phase 1.5 (golden-path exemplar) is unblocked once the on-device DoD passes clear.
4. **Harden the template (Phase 1.5).** Extract the **golden-path exemplar** + reusable scaffolding (feature folder shape, query/store/db conventions, screen/test/e2e skeletons). Update the Architecture Book to match what these 3 actually taught us. Record pattern decisions as ADRs.
5. *(Optional, if slack)* begin the **calendar spike** pre-read — it's our biggest unknown.

## Exit criteria

- All three features pass the full DoD on both platforms.
- Golden-path exemplar documented; scaffolding exists; a new feature can be started by copying it.
- Architecture Book reflects reality (not Phase-0 guesses); ADRs cover the core patterns.
- Any rule changed during this phase is logged in the Rule changelog.

## Risks & decisions

- **Don't over-freeze.** The point of three features is variety before blessing the pattern — if A's pattern doesn't fit C, that's the signal to generalize, not to force it.
- Personal events is intentionally **CRUD-only** here; its calendar overlay lands in [Phase 05](./05-personal-data.md) once the timeline exists. Clean seam, on purpose.
- Resist gold-plating: these are real features, but their job is to *teach the template*.
</content>
