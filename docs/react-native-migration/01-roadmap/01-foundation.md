# Phase 01 — Foundation (the walking skeleton)

> **Goal:** the codebase comes alive. Every cross-cutting system is wired and **green in CI**, and one trivial screen (**splash**) is taken through the *entire* Definition of Done on both platforms — so the polish we do is the foundation, not a feature.
>
> **Depends on:** nothing (kickoff). **Modules touched:** `splash`, plus all infra.
>
> This is Phase 0 in [`migration-approach.md`](../00-exploration/migration-approach.md) §4.

## Rough steps

1. **Scaffold `mobile`** as a **standalone npm project** (sibling of `server/` and `app/` — not a workspace member; see migration-approach §3 and the scaffold change's design D7). Latest **stable** Expo SDK ([K-1](../00-exploration/migration-approach.md#8-resolved-knobs-phase-0-kickoff-decisions)), New Arch + Hermes, TS strict, `expo-dev-client`. ✅ *Done (scaffold-mobile-expo, SDK 56).*
2. **Min OS targets** set to iOS 15.1 / Android API 24 ([K-2](../00-exploration/migration-approach.md#8-resolved-knobs-phase-0-kickoff-decisions)). ✅ *Done — effective floors iOS 16.4 / API 24 (SDK 56's own iOS minimum prevails; K-2 revisit flagged in §8).*
3. **API client:** Orval reading the server's OpenAPI spec → RN-owned TanStack Query hooks. (Web stays separate.)
4. **Lint/format + first custom ESLint rules:** no hardcoded strings, a11y props on touchables, no direct `@react-navigation/*` imports, import-boundary rules. Pre-commit + CI. ✅ *Done (add-mobile-lint-format) — all rules landed via core rules + existing plugins (no hand-rolled rule needed); also gained no-raw-fetch/no-axios (the data-layer R-1 debt) and Prettier aligned to web/server. The strings + a11y lint rules landed here ahead of schedule; their runtime halves followed — i18n wiring in step 6 (add-mobile-i18n) and a11y semantics + CI proof in step 7 (add-mobile-a11y), each with its own Architecture Book section.*
5. **Test harness:** Jest + RNTL green; **Maestro e2e green on iOS *and* Android** (even if it only asserts the splash renders).
   - *CI E2E strategy (landed):* the native E2E jobs are slow by nature (cold native build + device boot, ~20–30 min each — toolchain cost, not app size), so they run **on-demand**: PRs only with the `run-e2e` label, always on `main`/`production` when `mobile/**` or `openapi/**` changed. Split out of the monolith `build.yaml` into `ci-mobile-e2e.yml` (see `.github/workflows/`).
   - **Deferred — CI E2E caching speedups (not yet done):** ① AVD snapshot caching for the Android emulator (skip cold boot), ② iOS DerivedData cache keyed on the lockfile (point `xcodebuild -derivedDataPath` outside the `prebuild --clean` tree so it survives), ③ confirm the Gradle *build* cache (not just deps) is active. Realistic gain ~Android 28→18 min, iOS 18→10 min on warm caches. Orthogonal to the on-demand split above; pick up when E2E run time becomes a friction point.
6. **i18n** wired (FR + EN), no-hardcoded-strings rule live. ✅ *Done (add-mobile-i18n) — i18next + react-i18next + expo-localization, device locale with EN fallback, flat greppable + `tsc`-typed keys (FR/EN parity enforced at compile time), one proof test; all six `TODO(i18n-step-6)` suppressions removed; tabs reshaped (Accueil/Profil, Calendar deferred). See the Architecture Book i18n section.*
7. **a11y** lint rules live. ✅ *Done (add-mobile-a11y) — runtime half of step 7: heading semantics encoded in `ThemedText` (`type="title"`/`"subtitle"` → `accessibilityRole="header"`, caller override wins), schools loading/error exposed as accessible status, one CI proof test asserting the header role resolves in the rendered accessibility tree (negative + explicit-role-wins paths covered). The four `react-native-a11y` touchable rules stay live-but-unexercised until the first interactive control; everything lint can't encode (Dynamic Type, touch targets, meaningful labels, manual screen-reader passes, reduced motion, contrast) recorded as prose with reason + owner. See the Architecture Book a11y section.*
8. **Firebase** via `@react-native-firebase/*`: Crashlytics + Analytics verified end-to-end (an event and a forced test crash both arrive). Reuse the Flutter `google-services.json` / `GoogleService-Info.plist`. ✅ *Done (add-mobile-firebase) — RNFirebase v24 modular API behind a thin `src/firebase/` seam (`logEvent`/`recordError`/`crashTest`); **one Firebase project per environment** (dev variant → `timecalendar-dev`, prod → the Flutter app's `timecalendar-samuelprak`), variant-switched `googleServicesFile`; iOS `useFrameworks: "static"`; `crashlytics_debug_enabled`; native auto-init means no JS init + a free global JS error handler; a `__DEV__`-only `FirebaseDebugPanel` is the verification surface and the first live exercise of the a11y touchable rules; one CI proof test (wrapper→SDK). **The console-arrival half is a manual on-device step** (DebugView + Crashlytics dashboard) gated on the `.dev` apps being registered and the four committed config files placed in `mobile/firebase/` — see its README. See the Architecture Book Firebase section.*
9. **Storage seams:** MMKV + expo-sqlite/Drizzle initialized with a migration runner. (Schemas come with features; the *runner* exists now.) ✅ *Done (add-mobile-storage) — two swappable seams (`@/storage` over react-native-mmkv v4/Nitro for key-value; `@/db` over expo-sqlite + drizzle-orm), a non-hook startup `runMigrations()` over `drizzle-orm/expo-sqlite/migrator` (failure → `@/firebase`) applying a **committed but empty** bundle (runner proven, zero feature tables — R-2), the SQL-bundling toolchain (`drizzle.config.ts` + `generate:migrations` + created `babel.config.js`/`metro.config.js` for `.sql`), a new seam-import lint boundary, and two CI proof tests (MMKV round-trip + runner wiring). Both ride the existing iOS `useFrameworks: "static"` — no new build-properties. The `useMigrations()` loading-gate is the pattern features inherit. See the Architecture Book Storage section.*
10. **Theming + native-chrome wrappers:** design tokens; Expo UI / native-tabs / glass stubbed **behind our own abstractions** so they're swappable as those alpha APIs churn. ✅ *Done (add-mobile-theming) — a first-class typed token layer under `src/theme/` (colors light/dark, spacing, `Radii`, typography) consolidating the loose `constants/theme.ts`, with `useTheme` and the `Themed*` heading-role contract repointed unchanged; our own native-chrome wrappers under `src/components/chrome/` (`NativeTabs` themed from `@/theme`, `GlassSurface` centralizing the iOS-26+/fallback Liquid-Glass decision, an `@expo/ui` boundary-only seam — D6, R-2); a `no-restricted-imports` chrome-boundary lint banning the three alpha APIs outside `chrome/` (mirrors mutator/storage seams); WCAG-AA contrast pairs documented in `tokens.ts` (the DoD's manual-review artifact — runtime checker deferred); one CI proof test (token resolves light/dark + GlassSurface fallback renders). No new deps, no `app.config.ts` change. See the Architecture Book "Theming & native-chrome" section.*
11. **EAS** Build/Submit/Update configured; **app installs on a real device**; internal distribution channel set up (dogfooding starts here). ⏳ *Config half done (add-mobile-eas) — `mobile/eas.json` with three profiles (`development` simulator+APK / `preview` production-identity internal device build / `production` store, channels `preview`+`production`) keyed to `APP_VARIANT`, a secret-free `submit` skeleton, `expo-updates` wired with `runtimeVersion: { policy: "fingerprint" }` and the `updates.url`/`extra.eas.projectId` seam (placeholder until `eas init`); ADR 006; CI deliberately untouched (EAS stays human-invoked); no Jest proof test (D8 N/A — config, not runtime). See the Architecture Book "EAS / distribution" + `mobile/EAS.md`. **Not fully ✅** — the device-install / internal-channel half is irreducibly human: `eas login`+`eas init`, Apple/Google credentials, the real `eas build`/`submit`/`update`, and TestFlight/Play-internal setup are in `docs/react-native-migration/inbox/2026-06-13-eas-credentials.md`.*
12. **Create the 5 living artifacts** (Architecture Book, ADR log, DoD, Rule changelog, golden-path placeholder) — even mostly empty. The **Architecture Book already exists at `.claude/rules/mobile/`** (born with the scaffold) — the remaining four join it there; do **not** create a second book elsewhere. ✅ *Done (add-mobile-living-artifacts) — the four remaining artifacts now sit beside the book: `decisions/` (README + template + K-1…K-5 authored as real ADRs 001–005, including the K-1/K-2 revisits that already fired — the Phase 0 exit criterion), `definition-of-done.md` (the §5 checklist enriched with every obligation the book parked on it — manual screen-reader passes, touch targets, contrast, reduced motion, K-3 coverage), `architecture-changelog.md` (backfilled with the seven prior rule eras + this change), and `golden-path.md` (an honest Phase-1.5 signpost, not a copy-me template). The book gained a "The five living artifacts" pointer section and its forward-references now resolve. Docs-only; tsc/lint/Jest stay green.*
13. **Splash screen** built pixel-perfect / native-correct through the full DoD. ⏳ *Code half done (add-mobile-splash) — a JS animated splash overlay (`src/components/splash-screen.tsx`) over the existing static native splash: `expo-splash-screen`'s `preventAutoHideAsync()` (global) holds the native splash until JS is ready, the overlay continues it with brand from `@/theme` tokens + `t("app.name")` (light/dark correct), `hideAsync()` runs the native→JS handoff, and the overlay fades out (or cuts under reduced motion) once a `useAppReady()` readiness gate resolves. The gate (`src/hooks/use-app-ready.ts`) coordinates i18n + fonts + the migration runner and **always resolves** (synchronous today; a load-bearing watchdog timeout caps any future stalled gate). **Reduced motion** is honored in the component (the app's first animation — `AccessibilityInfo.isReduceMotionEnabled`, R-1: not lint), discharging the a11y section's recorded deferral. One CI proof test (localized brand renders, accessible status resolves, both reduced-motion branches, dismissal); prebuild validated. See the Architecture Book "Splash" section. **Not fully ✅** — the irreducibly on-device DoD axes (manual VoiceOver/TalkBack, on-device pixel/native-correctness on both platforms, real-device reduced-motion, contrast eyeball, low-end-Android jank, Crashlytics arrival, Maestro-through-splash) are in `docs/react-native-migration/inbox/2026-06-13-splash-dod-manual.md`. Phase-0 exit is gated on that manual pass.*

## Exit criteria

- App builds and ships to a real device via EAS internal channel.
- CI green: types, lint (incl. custom rules), unit, Maestro on both platforms.
- Crashlytics + Analytics confirmed receiving data.
- Splash passes **every** DoD item on iOS and Android.
- All 5 artifacts exist; first ADRs written (K-1…K-5 become real ADRs here).

## Risks & decisions

- **This is the highest-leverage phase** — every choice here is copied by every later feature. Spend the time.
- Native-chrome APIs are alpha; the wrapper layer (step 10) is the insurance against their churn.
- Don't build feature schemas yet — resist scope creep. The skeleton walks; it doesn't run.
</content>
