# 016 — Confirm school groups as a set

## Status

Completed product-flow record.

School-group leaves are multi-select toggles. An explicit confirmation persists the full,
non-empty identity set and dismisses the onboarding stack. Branch nodes only expand or
collapse. Pending selection remains local UI state.
