# 012 — Use the Expo UI date/time picker

## Status

Completed implementation record.

Personal-event forms use Expo UI's native `DateTimePicker` through the chrome seam, not
`@react-native-community/datetimepicker`. Revisit if native behavior or supported-platform
accessibility cannot meet the product requirements.
