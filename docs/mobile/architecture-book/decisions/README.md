# ADR log — TimeCalendar mobile

One short record per architectural decision: context, choice, alternatives,
"revisit if…". One of the [five living artifacts](../architecture.md#the-five-living-artifacts);
sibling of the Architecture Book, source of truth for *decisions* the way the book
is source of truth for *rules*.

## The process (mirrors migration-approach §7)

A load-bearing decision (R-4 triage: affects patterns reused across features) earns
an ADR. To add one:

1. Copy [`TEMPLATE.md`](./TEMPLATE.md) to `NNN-kebab-title.md` (next free number below).
2. Fill Context / Decision / Consequences / Revisit-if; set Status.
3. Add the row to the index.
4. If the decision changes a rule, update the Architecture Book **and** append to the
   [Rule changelog](../architecture-changelog.md) — the act of changing rules is itself recorded.

We expect to revise. A fired revisit is recorded **in place** (dated, in the ADR's
Status) — superseding an ADR means writing the new one and marking the old
`Superseded by NNN`, never deleting it.

## Index

| # | Title | Status | Revisit trigger |
| --- | --- | --- | --- |
| [001](./001-sdk-target.md) | SDK target: latest stable Expo SDK at scaffold | Accepted · revisit fired 2026-06-12 (scaffolded directly on SDK 56) | A future SDK forces a deferred breaking change, or a beta capability becomes load-bearing pre-GA |
| [002](./002-minimum-os.md) | Minimum OS: iOS 15.1 / Android API 24 | Accepted · revisit fired 2026-06-12 (effective floors iOS 16.4 / API 24) | Install base below the floors, or an SDK raises its own minimum again |
| [003](./003-coverage-threshold.md) | Coverage: 90% logic / 70% global, CI-enforced | Accepted · **enforced 2026-06-14** (wired by Settings prefs, TIM-130) | The gate drives cargo-cult tests rather than catching regressions |
| [004](./004-phase-1-feature-order.md) | Phase 1 feature order: Settings → Personal events → School selection | Accepted · not started | A dependency forces a different order, or a feature is too thin for its axis |
| [005](./005-calendar-spike.md) | Calendar spike: 3-day read-only spike opens Phase 2 | Accepted · not started | The spike clearly succeeds or fails early |
| [006](./006-eas-distribution.md) | EAS distribution: fingerprint runtime policy, human-invoked builds (CI untouched) | Accepted | Manual dogfood builds become a friction point (wire `.eas/workflows/`), or fingerprint forces rebuilds so often OTA stops paying off |
| [007](./007-drop-web-target.md) | Drop the web target: iOS + Android only | Accepted | A real web roadmap appears (a genuine browser deliverable, not the template default) |
| [008](./008-brand-color.md) | Brand color: adopt the Flutter pink hue as the `primary` token | Accepted | A designer-driven palette change, or a real surface fails the brand-color contrast |
| [009](./009-settings-feature-prefs.md) | First feature folder; Settings owns app preferences, consumed by infra seams | Accepted · **revisit fired 2026-06-14** (TIM-135; infra→feature edge resolved = allowed, B-4) | A **2nd** feature needs cross-cutting prefs (would re-open promote-to-infra) |
| [010](./010-expo-ui-chrome-wrapper.md) | `@expo/ui` adopted behind the chrome wrapper seam; universal entry the default | Accepted | A control needs a platform-specific entry / diverges, or `@expo/ui` proves unstable enough to warrant a fallback behind the same wrapper |
| [011](./011-personal-event-storage.md) | Personal-event storage: dates TEXT ISO-8601 UTC, color `#RRGGBB` TEXT, uid from `expo-crypto` | Accepted | A query/perf need genuinely requires numeric timestamps, or the Flutter wire format changes before the Phase-09 importer runs |
| [012](./012-personal-event-datetime-picker.md) | Date/time picker: `@expo/ui`'s own `DateTimePicker` behind the chrome wrapper (not `@react-native-community/datetimepicker`) | Accepted | `@expo/ui`'s control proves unstable enough to warrant the RNC fallback behind the same wrapper, or a control needs a platform-specific `@expo/ui` entry |
| [013](./013-query-persister-and-policy.md) | Offline query cache: a sync TanStack Query persister over the MMKV `@/storage` seam + the now-earned query policy | Accepted | The persisted cache grows large enough that sync MMKV writes jank (→ async/SQLite persister behind the seam), a read needs offline mutation queuing, or the query policy proves wrong for a more dynamic read |
| [014](./014-layered-feature-module-pattern.md) | The layered feature-module pattern: `src/features/<feature>/<layer>/` + sublayer/feature barrels + seam boundaries | Accepted · **lint landed 2026-06-14** (TIM-135 encoded B-1…B-4) | A *future* boundaries-encoding adjustment wants to change the shape, or a feature's axis needs a sublayer the pattern doesn't name |
| [015](./015-onboarding-flow-shape.md) | Onboarding flow shape: presentation-only `onboarding/` feature folder, welcome-first Stack, reachable but not a startup gate | Accepted | The calendar/home step wants the first-launch gate, the QR/iCal steps need onboarding to own more than presentation, or a designer multi-step intro replaces the single welcome surface |
| [016](./016-school-group-multi-select-commit.md) | School-group selection: multi-select committed by an explicit confirm, dismissing the onboarding Stack | Accepted | The calendar subscription step needs the confirm to PUT `SetSchoolGroupDto`, a school needs single-select group semantics, or ADR 015's deferred destination changes where the dismissal lands |
| [017](./017-qr-scan-camera.md) | QR scan: `expo-camera` for the camera surface, one `calendar-sources/` feature folder, no chrome wrapper, the QR yields a URL | Accepted | `expo-camera` proves insufficient (→ vision-camera), a camera surface needs a chrome wrapper, the server needs raw `webcal://` / a non-URL QR envelope, or a second input surface shows the single-folder shape doesn't fit |
| [018](./018-user-calendar-storage.md) | User-calendar storage: Drizzle (not MMKV) for the durable `user_calendars` token store; schema mirrors the Flutter `toDbMap()` verbatim (importer fidelity), `id`-is-record-key, dates TEXT ISO-8601 UTC, `visible` boolean | Accepted | A query/perf need requires numeric timestamps, the Flutter wire format changes pre-importer, a genuinely non-relational identity blob appears (re-weigh MMKV), or `visible` earns a toggle UI |
| [019](./019-calendar-rendering-adopt-calendar-kit.md) | Calendar rendering (Phase-04 spike outcome): ADOPT `@howljs/calendar-kit` v2 for the day/week timeline behind a seam, salvage the overlap/time-grid primitives, build agenda + home on them | Accepted (closes ADR 005's adopt/fork/custom gate) | calendar-kit fails the low-end-Android frame-rate bar on real hardware, breaks on a future SDK with a lagging maintainer, the brand surface fights `theme`/`renderEvent`, or the salvaged primitives outgrow the library (→ custom behind the unchanged seam) |
| [020](./020-calendar-kit-seam.md) | Calendar-kit seam form: a chrome-wrapper (`src/components/chrome/calendar-kit.tsx`) + `no-restricted-imports` ban — justified by swap-reversibility (not alpha churn, unlike ADR 010) | Accepted | A 2nd calendar renderer wants the wrapper (promote the API), calendar-kit is dropped behind the seam (ADR 019 revisit — API stays, body swaps), or the `chromeAlphaImportPatterns` constant outgrows the "alpha" name (rename) |
| [021](./021-calendar-event-storage-and-sync.md) | Calendar-event storage & sync: verbatim `calendar_events` schema (TEXT-ISO-UTC, importer fidelity, building on ADR 011/018); JSON columns (teachers/tags/fields) as plain TEXT with defensive mappers (not Drizzle `mode:"json"`); a transactional drop+replace; `type` stored verbatim; recoverable-fetch (isError) vs. crash-worthy-write (recordError) observability split | Accepted | A query/perf need requires numeric timestamps, the Flutter wire format changes pre-importer, a server tag/fields shape breaks the defensive decode, synced data outgrows the `useLiveQuery`/`SectionList` (perf trigger), or incremental/delta sync or an offline write queue is needed |
| [022](./022-home-ia-today-view.md) | Home tab IA: the Home tab becomes the today / next-up view (merging synced + personal events via the unchanged `useCalendarEvents` seam, the salvaged overlap engine's first rendering consumer); the standalone personal-events list relocates to a Profile-reached `/personal-events` route (create/edit/delete preserved, not dropped) | Accepted | The post-onboarding first-launch gate (ADR 015) lands on the Home tab, a designer-driven home layout replaces the single today view, or the personal-events list earns its own tab |
| [023](./023-hidden-events-storage.md) | Hidden-events storage: MMKV (not Drizzle) for the durable hidden-events set; a single verbatim `{ uidHiddenEvents, namedHiddenEvents }` blob mirroring the Flutter `toMap()` (importer fidelity); filtered at the unchanged events-source seam over the merged list (Flutter parity), hide action synced-only | Accepted | The hidden set becomes relational (per-calendar hiding / history / perf), the Flutter wire format changes pre-importer, the same-name-hides-a-personal-event parity surprises users, or a per-calendar `visible` filter lands |
| [024](./024-event-checklist-storage-and-surfacing.md) | Event-checklist storage & surfacing: a 4th Drizzle table `checklist_items` mirroring the Flutter `toMap()` verbatim (TEXT-ISO-UTC, importer fidelity; `eventUid` a soft ref with NO FK so a checklist survives a sync drop+replace); **delete is HARD, not soft** (`deletedAt` is a vestigial wire field kept for fidelity only — verified against Flutter, correcting the brief); reorder is accessible move-up/down (zero new dep); and a UNIFIED event-details screen serves BOTH personal and synced events (the `eventRoute` discriminator flipped, the personal-event edit form reached via an Edit header action) | Accepted | A query/perf need requires numeric timestamps, the Flutter wire format changes pre-importer, a product decision wants real soft-delete (undo/trash), the checklist list grows long enough to earn a drag-reorder dep, or the unified-details routing needs to split the two kinds again |
| [025](./025-calendar-tab-three-tab-ia.md) | Three-tab IA: add a **Calendar** tab (Home · Calendar · Profile, Flutter parity), giving the Phase-04 day/week/agenda surface a front door — it was deep-link-only (`timecalendar-dev://calendar`, Maestro). The `/calendar` route moves into the `(tabs)` group (path unchanged → deep link/Maestro intact); a third `NativeTabs.Trigger` between `index` and `profile`. Supersedes-in-part ADR 022's two-tab IA / its "no third tab" rejection (which was about a *personal-events* tab) | Accepted | A designer-driven nav (center FAB, "more" overflow, custom tab bar) replaces the native tab bar, or a 4th primary surface earns a tab and forces an overflow decision |
| [026](./026-fcm-messaging-seam.md) | FCM push-receive grown into the `@/firebase` seam (Phase 06 Ship A): four lazy-native helpers (`requestNotificationPermission`/`getFcmToken`/`onForegroundMessage`/`onFcmTokenRefresh`) + the **iOS APNS-token-FIRST** rule (never `getToken` before a non-null `getAPNSToken`), one **top-level `setBackgroundMessageHandler`** as the single documented no-native-on-import exception (loaded via a side-effect `import "@/firebase"` in the entry, mirroring i18n), `@react-native-firebase/messaging` v24 + entitlement/background-mode/`POST_NOTIFICATIONS` wiring, and the **re-affirmed Expo-Push rejection** (FCM + firebase-admin, server unchanged) | Accepted | A messaging pod breaks under static frameworks (`ios.forceStaticLinking`), RNFB drops the top-level background-handler requirement (reconsider the side-effect import — do NOT remove it before then), a 2nd top-level native need appears (re-weigh "exactly one exception"), or the transport decision reopens (Expo Push) |
| [027](./027-fcm-subscription-registration.md) | FCM-token-to-backend registration (Phase 06 Ship B): a **new `src/features/notifications/`** module (not Settings, not `@/firebase`) wrapping the **already-generated** `PUT /notification-subscription` client; the API is **PUT-only (no GET)** so the **local MMKV store is the source of truth** (flat `notifications.*` keys, total parsers, `nbDaysAhead` clamp [1,30], defaults `immediately`/`7`/`true`), **PUT idempotently** on every change + every token-refresh ("round-trips" = the PUT succeeds); first-PUT after a non-null token (mirrors Flutter `subscribeDelay`), **zero calendars still PUTs `calendarIds: []`** (so the server can prune), `calendarIds` = the `user_calendars` row `id` (server id, NOT the token); a failed PUT → `recordError` + accessible Retry. No native/dep/schema change | Accepted | The server gains a GET (re-weigh local-source-of-truth) or a delete endpoint (zero-calendars could DELETE not PUT `[]`); the `nbDaysAhead` default / permission placement proves wrong on device; a 2nd consumer needs the registration concern; or the PUT stops being idempotent |
| [028](./028-fcm-tap-routing.md) | FCM notification **tap-through routing** (Phase 06 Ship C): a **pure** `parseNotificationRoute` (server `calendar_changed` data message → `{ event, uid }` for `NEW`/`EDIT`, `{ calendar }` for `CANCEL`, `null` otherwise — missing data/wrong action/malformed JSON→`recordError`/missing uid all → null; **no React/Expo/native**, branch-testable) + a once-mounted `useNotificationTapRouting` dispatcher wiring the **three app states**: foreground (`onForegroundMessage`) → **refetch only, no nav** (Flutter parity); background tap (`onNotificationTap`) + killed/cold-start (`getInitialTap`, in a ref-guarded mount effect so the `<Stack>` is mounted) → **refetch then navigate**. Two new lazy-native seam helpers (`onNotificationTap`/`getInitialTap`), **R-2** only-`calendar_changed`, the uid-match assumption degrades to `useEventDetails` not-found. Reuses `useSyncCalendars` (no new sync path) + the `event-details/[uid]` route; no server/Orval/native change | Accepted | The server grows a new data-message action (own ship); a cold-start `router.push` races the navigator on device (wire the `rootNavigationState` readiness gate); the change-detection uid and sync DTO uid diverge in practice; a foreground push warrants an in-app banner over a silent refetch; or Ship D's local reminders need to share this dispatch path |
| [029](./029-local-reminders-deferred.md) | **Local reminders (`expo-notifications`) deferred** (Phase 06 Ship D): server push (ADRs 026–028) covers all **synced** calendars; stacking local reminders there is the forbidden double-stack. The one gap is **personal-event** reminders (device-local; the server never sees them) — but the in-production **Flutter app has no local notifications at all** (no `flutter_local_notifications`, no reminder field on `personal_event`), so building them now is net-new scope, not parity (**R-2**). **Not built**; recorded as future debt in the inbox. No dependency/permission/native/schema change | Accepted (records a non-adoption) | Users ask for personal-event reminders, a future phase adds a device-local event type whose value needs a reminder, or reminders become a product differentiator — then build as a dedicated feature + ADR, reconciling the permission with the existing FCM grant |

ADRs 001–005 are the Phase 0 kickoff knobs K-1…K-5 from migration-approach §8,
authored as real ADRs here (the Phase 0 exit criterion: "first ADRs written —
K-1…K-5 become real ADRs"). §8 is their origin record; this log is their canonical
home going forward.

## Pending lifts

Some change-local ADRs already live **inside their archived change** and are lifted
into this log (as the next free numbers, 006+) when they are next revised — moving
an archived change's file now would rewrite history in the archive, and these are
already authoritative where the book cites them.

| Where it lives now | What it decides | Lift when |
| --- | --- | --- |
| `openspec/changes/archive/2026-06-12-add-mobile-api-client/adr-001-committed-spec-seam.md` | The committed-spec seam (`openapi/openapi.json` as the single server↔mobile contract). Authoritative in the Architecture Book "Data layer" section. | Next revised, or when the contract seam itself changes. |
