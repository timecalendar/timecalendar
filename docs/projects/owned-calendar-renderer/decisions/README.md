# Architecture decision index

No project-local architecture decision exists yet because the product contract is not approved and
the architecture comparison has not begun.

After product approval, create stable `DNN-<slug>.md` records here for choices that constrain
implementation. At minimum, the design must resolve the six architecture rows named in
[design.md](../design.md): bounded visible work, imperative API shape, runtime constraints,
animation/gesture responsibilities, native/rendering dependency evidence, and dependency
governance. Each choice needs real alternatives, measured evidence, tradeoffs, affected `P*`
outcomes, and explicit human approval.
