# Epic and ticket index

No implementation epics or tickets are canonical yet. The project is awaiting approval of
[product.md](../product.md), and technical design plus all active architecture decisions must be
approved before the delivery decomposition is created.

When those gates pass, each `ENN-<slug>/README.md` will describe an independently demonstrable
outcome and contain its tracker-neutral `TNN-<slug>.md` ticket documents.
