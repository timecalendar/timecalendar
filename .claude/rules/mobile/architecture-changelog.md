# Rule changelog — TimeCalendar mobile

One of the [five living artifacts](./architecture.md#the-five-living-artifacts): a dated,
**append-only** (newest last) log of every change to the rules — the [Architecture Book](./architecture.md)
hub + topical files or the [Definition of Done](./definition-of-done.md). Per migration-approach §7,
the act of changing a rule is itself recorded here. Each entry: date · change-slug · what moved · why ·
pointer link. **Backfilled** below the marker (seven pre-changelog eras, authored retroactively from the
landed changes); everything below the live marker is appended live.

---

### Backfilled — rule eras that landed before this changelog existed

- **2026-06-12 · `scaffold-mobile-expo`** — Seeded the scaffold-time rules: Expo SDK 56 (New Arch + Hermes), `mobile/` as a standalone npm project, TS strict flags, CNG native projects, `APP_VARIANT` identity, OS floors (iOS 16.4 / API 24, K-2 revisit fired). *Why:* the foundation's first rules. → [runtime.md](./runtime.md).

- **2026-06-12 · `add-mobile-api-client`** — Data layer: the committed-spec seam (`openapi/openapi.json`), Orval TanStack Query client (committed), the single `customFetch` mutator (no axios), base-URL config, stock-defaults `QueryClient`. *Why:* one reviewable server↔mobile contract + one fetch seam. → [data.md](./data.md) ([pending-lift ADR](./decisions/README.md#pending-lifts)).

- **2026-06-12 · `add-mobile-test-harness`** — Testing: `jest-expo`, mock-at-the-`customFetch`-seam, coverage reported-not-gated; Maestro real-round-trip e2e + shared server lifecycle + dev-variant builds/network exceptions; the route-screen / non-tab-route navigation rules. *Why:* a green test floor + the navigation rules it surfaced. → [testing.md](./testing.md), [navigation.md](./navigation.md).

- **2026-06-12 · `add-mobile-lint-format`** — Lint & format: ESLint 9 flat config, Prettier-as-lint-error, zero-warnings, pre-commit; the rule inventory (no-hardcoded-strings, a11y-on-touchables, navigation ban, import boundaries, no-raw-fetch / no-axios). *Why:* encode the rules R-1 says must be encoded. → [lint-format.md](./lint-format.md).

- **2026-06-13 · `add-mobile-i18n`** — i18n runtime: module-scoped i18next + react-i18next + expo-localization, device-locale / EN fallback, flat greppable keys, `tsc`-typed FR/EN parity, a CI proof; scaffold reshaped to real tabs (Accueil / Profil). *Why:* pay off no-hardcoded-strings with a real runtime. → [i18n.md](./i18n.md).

- **2026-06-13 · `add-mobile-a11y`** — a11y runtime: the heading-role contract in `ThemedText`, accessible async status, a CI proof; recorded as prose what lint can't encode (Dynamic Type, touch targets, labels, screen-reader passes, reduced motion, contrast). *Why:* the runtime half of a11y + naming the DoD obligations. → [accessibility.md](./accessibility.md).

- **2026-06-13 · `add-mobile-firebase`** — Firebase: `@react-native-firebase/*` v24 modular API behind a thin `@/firebase` seam, one project per environment, iOS static frameworks, `crashlytics_debug_enabled`, the `__DEV__` `FirebaseDebugPanel`, a CI proof; discharged the `.dev` Firebase-registration deferral. *Why:* the first real instrumentation. → [firebase.md](./firebase.md).

---

### Live — appended as rules change

- **2026-06-13 · `add-mobile-living-artifacts`** — Created the four remaining living artifacts beside the book: the ADR log (README + template + K-1…K-5 as ADRs 001–005), the [Definition of Done](./definition-of-done.md), this changelog (backfilled), and the golden-path placeholder. *Why:* foundation step 12 — every later slice gets a real DoD, ADR log, and changelog. → [architecture.md](./architecture.md).

- **2026-06-13 · `add-mobile-storage`** — Storage: two swappable seams (`@/storage` over MMKV v4, `@/db` over expo-sqlite + drizzle), a non-hook startup migration runner over a committed-but-empty bundle, the SQL-bundling toolchain, and a new seam-import lint boundary; two CI proofs. *Why:* foundation step 9 — the persistence plumbing wired before the first feature. → [storage.md](./storage.md).

- **2026-06-13 · `add-mobile-theming`** — Theming & native-chrome: a typed token layer under `src/theme/` (consolidating the loose constants/hooks), swappable `src/components/chrome/` wrappers (`NativeTabs`/`GlassSurface`/`@expo/ui` boundary-only seam), a new chrome-boundary lint rule, WCAG-AA contrast pairs in `tokens.ts`, one CI proof. *Why:* foundation step 10 — the insurance layer against alpha-API churn + the token home. → [theming.md](./theming.md).

- **2026-06-13 · `add-mobile-eas`** — EAS / distribution: `mobile/eas.json` (three profiles split on `APP_VARIANT`), a secret-free `submit` skeleton, `expo-updates` with `runtimeVersion: { policy: "fingerprint" }`; EAS stays human-invoked (CI untouched). *Why:* foundation step 11 — configure so a human can dogfood on a real device. → [eas.md](./eas.md), ADR [006](./decisions/006-eas-distribution.md).

- **2026-06-13 · `add-mobile-splash`** — Splash: a JS animated overlay over the static native splash (`useAppReady()` readiness gate that always resolves; reduced-motion contract encoded in the component, discharging the a11y deferral), one CI proof + `jest/setup-splash.ts`; no new rule encoded. *Why:* foundation step 13 — the Phase-0 capstone, the first feature through the entire DoD. → [features.md](./features.md).

- **2026-06-13 · `drop-mobile-web-and-brand-theme`** — Web target dropped (iOS + Android only); single color-scheme seam (C1 `@/hooks/use-color-scheme`); tokenized RN nav theme (C2 `buildNavTheme`); pink brand `primary` token with re-verified WCAG-AA contrast; `BottomTabInset` removed. *Why:* prepare clean theming ground for Phase-1 Settings + shed the unused web surface. → [theming.md](./theming.md), [runtime.md](./runtime.md), ADR [007](./decisions/007-drop-web-target.md).

- **2026-06-14 · `add-mobile-import-order-lint`** — Lint & format: a new import/export-order rule (`eslint-plugin-simple-import-sort`, autofixable; canonical group order side-effect → builtins/third-party → `@/` → relative), one-time `--fix` sweep of 6 files; no ADR, no proof test. *Why:* lock in the previously hand-maintained import order before features multiply the surface. → [lint-format.md](./lint-format.md).

- **2026-06-14 · `add-mobile-settings-prefs`** — Phase-2 Feature A, data/logic only: the repo's first feature folder (`src/features/settings/prefs/`, two total-validated prefs over `@/storage`), a reactive `useStoredString` seam extension, wired into the C1 theme seam + i18n; the K-3 coverage gate flipped to **enforced**, landed green. *Why:* Phase-2 opens with Settings' data layer + discharges the C1/locale/K-3 loose ends. → [features.md](./features.md), ADR [009](./decisions/009-settings-feature-prefs.md).

- **2026-06-14 · `add-mobile-settings-screen`** — Settings UI layer (A2): the first `@expo/ui` chrome wrapper (`chrome/expo-ui.tsx`, universal `Host`+`Picker`, discharging the theming-D6 deferral), a presentational screen + thin route + the first real product touchable, a proof test + Maestro flow. *Why:* Phase-2 Feature A's screen + first `@expo/ui` consumer. → [features.md](./features.md), [theming.md](./theming.md), ADR [010](./decisions/010-expo-ui-chrome-wrapper.md).

- **2026-06-14 · `add-mobile-personal-events-data`** (B1) — Phase-2 Feature B data layer: the first real `src/db/schema.ts` (`personalEvents`, Flutter-verbatim for importer fidelity), the first real migration (replacing the empty bundle), the `@/db` seam widened, the second feature folder (`personal-events/data/`), new dep `expo-crypto` (autolinks); K-3 gate green. *Why:* the first structured device-local schema + real migration. → [storage.md](./storage.md), ADR [011](./decisions/011-personal-event-storage.md).

- **2026-06-14 · `add-mobile-personal-events-ui`** (B2) — Phase-2 Feature B UI: reactive Home-tab list + create/edit/delete form route, form/validation logic in `personal-events/form/` (90%), the second `@expo/ui` chrome consumer (`DateTimePicker` subpath, no new dep), a custom color-swatch picker, observability ✅ wired (real write error path), a Maestro CRUD round-trip. *Why:* the first multi-field form + real write error path. → [features.md](./features.md), [theming.md](./theming.md), ADR [012](./decisions/012-personal-event-datetime-picker.md).

- **2026-06-14 · `add-mobile-school-selection`** (C1) — Phase-2 Feature C, the first server-data read flow: the earned query policy + a sync TanStack Query persister over the `@/storage` seam (`mmkvQueryStorage`), the `school-selection/` feature layer (`data/` + `store/`), nested onboarding nav; retires the `schools` harness → `.maestro/onboarding.yaml`. *Why:* the first server read + offline cache + nested onboarding. → [data.md](./data.md), [features.md](./features.md), ADR [013](./decisions/013-query-persister-and-policy.md).

- **2026-06-14 · `harden-mobile-golden-path`** (Phase 1.5) — Extracted the golden-path exemplar from the three pattern-establishment features (the common spine, every claim a pointer) + a reusable skeleton template tree at `.claude/rules/mobile/golden-path-template/` (no code generator); Architecture Book reconciled; no new rule encoded. *Why:* extract and bless the golden-path template the rest of the app copies. → [features.md](./features.md), [data.md](./data.md); [golden-path.md](./golden-path.md); ADR [014](./decisions/014-layered-feature-module-pattern.md).

- **2026-06-14 · `add-mobile-feature-boundaries-lint`** (TIM-135) — Lint & format: the feature-module import boundaries (B-1 `data/`-only-seam, B-2 no-self-barrel-cycle, B-3 barrel-entry-point, B-4 allowed infra edge) **now encoded** via `eslint-plugin-boundaries` v6, layered on the existing seam bans; two new devDeps (incl. an explicit `eslint-import-resolver-typescript`); fires ADR 009's parked revisit (infra→feature edge allowed). *Why:* encode-before-document (R-1) the last load-bearing boundaries that lived in prose. → [lint-format.md](./lint-format.md); ADRs [009](./decisions/009-settings-feature-prefs.md)/[014](./decisions/014-layered-feature-module-pattern.md); [golden-path.md](./golden-path.md).

- **2026-06-15 · `refactor-mobile-rulebook`** — Split the monolithic `architecture.md` into a lean **hub** (five artifacts · working rules R-1…R-6 · topical-file index) + **topical rule files** (`runtime`/`navigation`/`data`/`storage`/`lint-format`/`testing`/`i18n`/`accessibility`/`theming`/`firebase`/`eas`/`features`.md), each present-tense and pointer-style; relocated the golden-path skeleton template into the rules dir (`.claude/rules/mobile/golden-path-template/`); compressed this changelog; corrected the DoD coverage axis (reported→**enforced**). *Why:* the book outgrew one file (~15k words) and read as feature-by-feature history; the rules a new feature must follow were buried. **No rule content changed — structure + legibility only.** → [architecture.md](./architecture.md) (hub) + the topical files.

- **2026-06-15 · `relocate-feature-screens-to-ui`** — Feature **screens** moved out of `src/components/` into a new per-feature **`ui/` sublayer** (`src/features/<feature>/ui/`: Settings, Personal events' form + list, the School-selection onboarding pickers); **splash became a presentation-only feature folder** (`src/features/splash/`, just `ui/`; `useAppReady` stays in `src/hooks/`). Shared primitives/shell (`themed-*`, `color-swatch-picker`, `date-time-field`, `firebase-debug-panel`, `app-tabs`, `chrome/*`) stay in `src/components/`. The `ui/` sublayer needed **no new boundary rule** — it matches the existing `feature-sublayer` element (`src/features/*/*`), so B-1/B-2 govern it: screens consume **sibling sub-barrels** (B-2-clean), routes re-export through the `ui/` sub-barrel. The 90/70 coverage split (ADR 003) is preserved by excluding `ui/` from the broad logic glob (`src/features/*/!(ui)/**` — an extglob, since Jest thresholds are additive not most-specific-wins). Doc comments in `eslint.config.js` updated; **no rule-logic change**. Landed green (tsc + lint + 130 tests + coverage). **Extends ADR [014](./decisions/014-layered-feature-module-pattern.md)** — its open-sublayer-set revisit fired (`ui/` added), recorded in-place. *Why:* a feature's screens are feature-owned presentation, not shared components — co-locating them under the feature completes the layered module shape. → [features.md](./features.md), [lint-format.md](./lint-format.md), [navigation.md](./navigation.md), [golden-path.md](./golden-path.md); ADR [014](./decisions/014-layered-feature-module-pattern.md).

- **2026-06-15 · `add-mobile-onboarding-flow`** (Phase-3 ship 1) — A native-default brand/**welcome surface** as the onboarding entry, in a new **presentation-only `src/features/onboarding/ui/`** feature folder (the second `ui/`-only folder beside splash; ADR 014's open sublayer set already names `ui/`). The nested `onboarding` Stack is re-ordered **welcome-first**: `index` = welcome (`timecalendar-dev://onboarding`), the school picker moved to `onboarding/school`, `groups` unchanged (the `school-selection/ui/` screens are untouched — ship 2 grows the picker). The welcome CTA `router.push("/onboarding/school")`; the Profile entry relabeled "Get started"/"Commencer" (still `href="/onboarding"`). `mobile/.maestro/onboarding.yaml` **extended** — welcome → CTA → the live `GET /schools` read (the Phase-2 proof preserved, reached via the CTA). Onboarding is **reachable but not a startup gate** (the first-launch redirect is deferred to the calendar/home step; `isOnboardingComplete()` stands). **No new rule, no new dependency, no `app.config.ts`/native change** — the new UI rides the existing feature-boundary / i18n / a11y / coverage gates (the welcome screen is `ui/`, under the 70% floor). Landed green (tsc + lint + 134 tests + coverage). → [features.md](./features.md), [navigation.md](./navigation.md), [golden-path.md](./golden-path.md); ADR [015](./decisions/015-onboarding-flow-shape.md).

- **2026-06-15 · `add-mobile-school-picker`** (Phase-3 ship 2) — **Completed** the school / school-group picker, a narrow GROW of Phase-2 Feature C (TIM-134; the read seam, persister, identity-only store, nested nav untouched). Three gaps closed: (1) the **group step → multi-select with an explicit confirm-commit** — leaves are toggles into a pending set (accessible `selected` state), branches expand/collapse, one primary confirm persists the whole set through the **unchanged** identity-only store (`selectSchool` + `selectGroup([...set])`) with an empty-selection guard (**ADR [016](./decisions/016-school-group-multi-select-commit.md)**); (2) **accent-insensitive name-or-code search** behind a pure `data/search.ts` helper (`normalize` via Hermes `normalize("NFD")` + a combining-marks range strip `U+0300–U+036F` — **not** `\p{Diacritic}`, which Hermes mishandles; `code` projected into `SchoolListItem`), 90%-gated; (3) completion **dismisses the whole onboarding Stack** (`router.dismissTo("/onboarding")`) instead of `router.back()` stranding the user on the school list. New FR+EN keys (`onboarding.group.confirm`/`.confirmLabel`/`.empty.selectionGuard`; `nodeLabel` re-authored for toggle). `mobile/.maestro/onboarding.yaml` extended with a stable school-search step only (the multi-select/confirm stays Jest-proven — fixture-dependent leaf selectors, D5). **No new rule, no new dependency, no `app.config.ts`/native change** — new behavior behind the existing feature-boundary / i18n / a11y / coverage gates (the search helper is `data/`-90%-gated; the screens stay under the 70% floor). Landed green (tsc + lint + tests + coverage). → [features.md](./features.md), [data.md](./data.md); ADR [016](./decisions/016-school-group-multi-select-commit.md).

- **2026-06-15 · `add-mobile-qr-scan`** (Phase-3 ship 3) — The first **camera input method** (scan a QR to add a calendar). **New native dep `expo-camera`** (`~56.0.x`, SDK-aligned; lockfile updated): its native module **autolinks**, and `app.config.ts` `plugins` gains `["expo-camera", { cameraPermission, recordAudioAndroid: false }]` — the permission strings are config-shape, prebuild-verified (R-1); **`recordAudioAndroid: false` is mandatory** (the plugin defaults it to `true`, adding `RECORD_AUDIO` — QR never records audio). **The repo's first `src/features/calendar-sources/`** folder (named for the *concern* so ships 4/5 grow it in place): `data/` with a pure 90%-gated `parseScannedSource` (Flutter-verbatim http(s) passthrough, the one deliberate **`webcal://`→`https://`** reversible normalization, non-URL→`null`) + an **ephemeral** module-scoped scanned-source holder (NOT MMKV/Drizzle — ship 5 swaps it); `ui/` with the QR scanner screen (the `useCameraPermissions` lifecycle, QR-only `CameraView`, single-scan debounce, accessible permission/scan states) imported **directly** from `expo-camera` (**no chrome wrapper** — it's a stable GA module, not an alpha API). A welcome-screen "Scan a QR code" CTA + a thin `src/app/onboarding/qr-scan.tsx` route. **Observability ✅** (a thrown failure → `@/firebase` `recordError`; a recoverable non-calendar QR is not recorded). New `jest/setup-expo-camera.ts` suite-wide mock + a local controllable mock prove the scan→parse→state wiring (the camera can't be CI/Maestro-driven; the on-device + prebuild proof is `inbox/2026-06-15-qr-scan-dod-manual.md`). FR+EN keys; landed green (tsc + lint + 164 tests + coverage). *Why:* Phase-03 ship 3 — first camera input + the calendar-sources cluster home. → [runtime.md](./runtime.md), [features.md](./features.md); ADR [017](./decisions/017-qr-scan-camera.md).

- **2026-06-15 · `add-mobile-ical-import`** (Phase-3 ship 4) — The **second calendar-source input method** (add a calendar by **pasting an iCal URL**), **grown in place** in `src/features/calendar-sources/` (sublayers, not a new folder — ADR 017 D3). Import is a **server `POST /calendars { url } → { token }`** round-trip, not a client-side `.ics` parse — Flutter parity (`import_ical`'s `loadIcalUrl`; the server owns parsing). New `data/create.ts` — the feature's **first generated-hook import site** (B-1) wrapping the **already-committed** `useCalendarSyncControllerCreateCalendar` over the single `customFetch` mutator (builds `CreateCalendarDto { url: url.trim(), customData: null }`, resolves `{ token }`); a pure 90%-gated `data/validate-url.ts` returning a **localizable key** (`calendarSources.icalUrl.error.{empty,invalid}`), deliberately lenient (the server is the authoritative validator — Flutter has no client validation); a `ui/ical-url-screen.tsx` (labeled RN-core `TextInput`, submit, accessible importing / server-error + **Retry**) that on success stashes the **same ship-3 `ScannedCalendarSource { url }` into the same ephemeral holder** (`setScannedSource` — ship 5 swaps it for durable token storage) and dismisses. A welcome-screen "Add by URL" CTA + a thin `src/app/onboarding/ical-url.tsx` route. **Observability ✅** (a server create failure → `@/firebase` `recordError(error, "calendar-sources/ical-import")` + accessible error + retry; an invalid-URL pre-filter miss is inline, not recorded). Jest proves the validate→create→handoff wiring + server-failure + retry at the `customFetch` seam (real `QueryClient` + real generated mutation); a light `.maestro/ical-import.yaml` asserts render + reachability + empty-submit inline validation only (the real import isn't Maestro-driven — no seeded `.ics`; on-device pass `inbox/2026-06-15-ical-import-dod-manual.md`). FR+EN keys. **No new dependency, no `app.config.ts`/babel change, no ADR** (growth within ADR 017 D3 + the existing `data/` + form-validator patterns). Landed green (tsc + lint + 178 tests + coverage). *Why:* Phase-03 ship 4 — the second calendar-source input method. → [features.md](./features.md), [data.md](./data.md); ADR [017](./decisions/017-qr-scan-camera.md).

- **2026-06-16 · `add-mobile-calendar-identity-persistence`** (Phase-3 ship 5/5, the load-bearing identity ship) — Made the calendar-subscription store **durable**: the **token IS the user's identity, no server backup**. The **second real Drizzle table** since `personal_events` — `userCalendars` (SQL `user_calendars`) — mirroring the Flutter `UserCalendar.toDbMap()` wire format **verbatim** for **importer fidelity** (a Phase-09 target): `id` TEXT primaryKey (the sembast record key = upsert identity, **not** the token), `token` TEXT notNull (the irreplaceable subscription identity), `name`, nullable `schoolName`/`schoolId`, `lastUpdatedAt`/`createdAt` TEXT UTC ISO-8601, `visible` `integer({ mode: "boolean" }).default(true)`. The **second real migration** (`0001_loving_bug.sql` `CREATE TABLE user_calendars`, a second `_journal.json` entry, updated `migrations.js` — all committed, fresh-clone-no-codegen); the runner applies both, the `migrate.test.ts` proof unchanged. The `@/db` seam re-exports `userCalendars` (no new operator — `eq` suffices, R-2). The full `data/user-calendars/` sub-module (pure `rowToCalendar`/`calendarToRow` + `fromCalendarForPublic` mappers; the repository `findAll`/`getById`/`getByToken`/`upsert`-by-id/`remove`/`setVisible`; `newId()`; the reactive `useUserCalendars()`; the shared `useAddCalendar`/`addCalendarFromUrl` persist seam: POST `/calendars` → resolve `GET /calendars/by-token/{token}` → `upsert`). **Ships 3/4 rewired to persist durably** — the QR-scan + iCal-import success paths now `upsert` a durable row; the **ephemeral `scanned-source.ts` holder removed** (its consumers gone). **Observability ✅** — the first calendar-sources write that can fail → `@/firebase` `recordError` + an accessible failure surface. Persistence tests are central: mappers (round-trip, **importer-fidelity verbatim**, canonical-UTC, null/boolean), repository query shape, persist wiring (success + failure at the `customFetch` seam), and a **restart-simulation** (a fresh repository module reads back a prior write through a stateful Map-backed `@/db` fake — the write-then-read-back contract); on-disk SQLite survival is the on-device manual pass (HUMAN: `inbox/2026-06-16-calendar-restart-durability.md` — no list UI ships, so no Maestro post-relaunch target). **No new dependency, no `app.config.ts`/native change** (a new SQLite table under the existing seam). **ADR [018](./decisions/018-user-calendar-storage.md)** records the Drizzle-over-MMKV call (MMKV genuinely weighed, rejected as a flat-KV hack for relational identity) + the verbatim-schema decision (mirroring ADR 011). Landed green (tsc + lint + tests + coverage). *Why:* Phase-03 ship 5 — durable calendar-token identity, the load-bearing ship the whole app's identity hangs on. → [storage.md](./storage.md), [features.md](./features.md); ADR [018](./decisions/018-user-calendar-storage.md).

- **2026-06-16 · `calendar-spike` (Phase-04 item 0, the K-5 / ADR 005 spike)** — Ran the time-boxed read-only calendar spike on the **real stack** (Expo SDK 56, RN 0.85.3, Reanimated 4.3.1 + worklets 0.8.3, gesture-handler ~2.31.1, New Arch, Hermes) and **resolved the adopt/fork/custom gate**. Quarantined throwaway code (outside `src/`, never shipped) booted **two** candidates side-by-side on the iOS simulator against a worst-case dense university week (a Tuesday 5-way overlap cluster): (a) **`@howljs/calendar-kit` v2.5.6** — **booted with no Reanimated-4 crash** (the feared upstream `EXC_BAD_ACCESS` did NOT occur), rendered correct dense-overlap column packing, a brand-tinted now-indicator, and custom `renderEvent` tiles — *disproving* the "RISKY/might-crash" hypothesis the research raised; and (b) a **custom** plain-Reanimated absolute grid driven by a pure, unit-validated **overlap-packing engine ported from Flutter** (`EventForUI.listFromEvents` → 5-way cluster yields 5 even columns; A/B/C → exact thirds). **Decision: ADOPT calendar-kit for the day/week timeline behind a feature seam, SALVAGE the overlap/time-grid primitives as our own pure 90%-gated modules (for the agenda list calendar-kit lacks + the home today-grid + fallback insurance), build agenda + home on them** — recorded as **ADR [019](./decisions/019-calendar-rendering-adopt-calendar-kit.md)** (closes the open gate ADR [005](./decisions/005-calendar-spike.md) parked; ADR 005 status annotated). Rationale: the library demonstrably clears the #1-risk bar on our exact stack today, is pure-JS/forkable and rides the OTA lane (no fingerprint bump), and the seam + salvaged engine keep a cheap fork/custom escape — so building custom now would re-pay the highest migration risk for parity the library already delivers (following the "likely custom" prior over the evidence the spike exists to gather). **Inboxed (human/device-only, never loop blockers):** the low-end-Android frame-rate verification (`inbox/2026-06-16-calendar-low-end-android-perf.md` — the exit-criterion bar; a CI emulator/iOS sim is not a low-end device) and the both-platform visual brand review (`inbox/2026-06-16-calendar-visual-brand-review.md`). **The spike itself ships only this ADR + docs to main** (no production code, no dependency yet — the rendering ship adds calendar-kit properly behind the seam with lint/coverage gates); the throwaway spike code is discarded. *Why:* Phase-04 item 0 — earn the calendar build-vs-buy decision with evidence; it reshapes the rest of the phase's worklist. → ADR [019](./decisions/019-calendar-rendering-adopt-calendar-kit.md), [005](./decisions/005-calendar-spike.md); roadmap [04-calendar-core.md](../../../docs/react-native-migration/01-roadmap/04-calendar-core.md).

- **2026-06-16 · `add-mobile-calendar-timeline`** (Phase-04 item 1, first half — the day/week timeline) — Implemented ADR 019's adopt decision: the **read-only day/week timeline** via `@howljs/calendar-kit` (`~2.5.6`, **pure-JS** — autolinks nothing, no `app.config.ts` plugin, **no EAS-fingerprint bump**; `luxon`/`rrule`/`lodash.*` transitives in the lockfile) behind a new **chrome-wrapper seam** `src/components/chrome/calendar-kit.tsx` (the single import site; `no-restricted-imports` bans `@howljs/calendar-kit` everywhere except `src/components/chrome/**`, re-set off there mirroring `@expo/ui`). A **`GestureHandlerRootView`** now wraps the app root (`src/app/_layout.tsx`) — calendar-kit requires it; app infra, not behind the seam. The new **`src/features/calendar/`** folder (`data/` + `ui/`): the **salvaged pure primitives** (`overlap-layout.ts` `layoutOverlaps` — the unbounded-column packing, 5-way→5 columns / A·B·C→thirds / freed-column reuse; `time-grid.ts` — the 7:00–21:00 Flutter-parity constants + minute→pixel/height/labels/now-indicator math), **owned regardless of the renderer** (ADR 019's salvage mandate, the fallback insurance), 90%-gated; the **domain `CalendarEvent`** (designed against the eventual sync `calendar_event.toDbMap()` model, **not persisted**) + the **events-source seam** `useCalendarEvents(range)` (fed this ship from a committed dense-week fixture + the personal-events read; the **sync ship swaps the source behind the unchanged hook**); the **brand-themed timeline screen** (`ui/calendar-screen.tsx`, 70% floor — the `theme` from `@/theme` tokens, now-indicator → brand `primary`, a day/week switch, accessible tiles/controls/empty state, read-only) + a thin `src/app/calendar.tsx` route (Stack sibling of `(tabs)`). New FR+EN keys; a suite-wide `jest/setup-calendar-kit.ts` mock (the mocked `CalendarBody` invokes `renderEvent` per event so the screen's event→tile/mapping/theme/label wiring is provable without the Reanimated grid — same posture as the `@expo/ui` picker). A `.maestro/calendar.yaml` asserting render + a fixture event title. **Observability ➖ N/A** (read-only, no crash-worthy write/throw — mirrors school-selection). **HUMAN-blocked (inboxed, never loop blockers):** the low-end-Android frame-rate + Reassure baseline pass (`inbox/2026-06-16-calendar-low-end-android-perf.md` — ADR 019's exit-criterion gate) and the both-platform brand visual review (`inbox/2026-06-16-calendar-visual-brand-review.md`). The seam form is recorded as **ADR [020](./decisions/020-calendar-kit-seam.md)** (the chrome-wrapper + lint ban, justified by **swap-reversibility** — NOT alpha churn, the load-bearing nuance vs. ADR 010); a new topical rule file [calendar.md](./calendar.md) lands. The agenda/planning list + calendar sync are scoped follow-ups that build on the salvaged primitives + the events-source seam this ship lands. Landed green (tsc + lint + 228 tests + coverage). *Why:* Phase-04 item 1 (first half) — the day/week timeline, the #1-risk surface, per ADR 019. → [calendar.md](./calendar.md), [features.md](./features.md), [theming.md](./theming.md), [lint-format.md](./lint-format.md); ADR [019](./decisions/019-calendar-rendering-adopt-calendar-kit.md), [020](./decisions/020-calendar-kit-seam.md).

- **2026-06-16 · `add-mobile-calendar-agenda`** (Phase-04 item 1b — the agenda/planning view) — Completed the day/week/**agenda** rendering surface per ADR 019's "build the agenda (the view calendar-kit lacks) on the salvaged primitives — the easy half." Added the **third in-place view mode** of the calendar screen (day → week → **agenda**), grown in `src/features/calendar/`: a **day-grouped list, NOT a timeline grid**, so it does **not** use calendar-kit. Two new pure 90%-gated `data/` helpers — `groupEventsByDay(events): AgendaDay[]` (`data/agenda.ts`, the agenda analog of `layoutOverlaps`: sort by `startsAt` with a stable `localeCompare` tie-break, bucket by **local** calendar day — local Y-M-D, mirroring Flutter `isSameDate`, NOT UTC, so a 23:30-local event lands on its own day — ascending; **deliberate divergence from the Flutter `events_for_planning_view_helper` `endsAt`-carry quirk**, recorded so it is not "fixed" back) and a locale-aware **display-only** date/time formatter (`data/format.ts` over `date-fns` — `formatDayHeaderParts` (uppercased short weekday + day number, Flutter `fullDayToShortDay`) + `formatTimeRange` (`"HH:mm – HH:mm"`, 24-hour, French-first); locale resolved from `i18next.language` → `AppLocale`; **roadmap item 6 pulled early**, the first real date-formatting need; display only — no parsing/rrule/Temporal). The presentational `ui/agenda-list.tsx` — a React Native core **`SectionList`** (**zero new dep** — D4) of day sections (heading-role day headers + themed event tiles: a `Radii.large` ~15px radius, a subtle shadow offset (0,3)/6% black/blur 15 Flutter-parity, the `#RRGGBB` event color as the tile's left accent border, a brand-`primary` now/upcoming indicator on the next-upcoming event), read-only (the tile is NOT a touchable — `accessibilityRole="text"`, the tap target lands with event details later). The screen widens `view` to `"day" | "week" | "agenda"`, adds a third accessible `tab`, and in the agenda branch computes a **bounded multi-day window** (the visible week) and reads the **unchanged `useCalendarEvents(range)` seam** — no seam/`CalendarEvent`/source change. **New deps `date-fns` v4 + `date-fns-tz` v3** (`npx expo install`-pinned) — **pure-JS**: autolink nothing, no `app.config.ts` plugin, **no EAS-fingerprint bump** (OTA lane), work under Jest unchanged; **not** a chrome-seam/banned import (a plain utility, unlike calendar-kit — no swappable rendering surface to localize). **`SectionList` over FlashList** (D4): the data is bounded (fixture + personal events over the visible week), virtualization buys nothing, FlashList v2 would be a fingerprint-bumping native dep for no gain (R-2); **recorded revisit trigger** — when sync widens the range to hundreds–thousands of events, swap FlashList v2 behind the **unchanged `AgendaList`** (owned by the sync ship's perf pass). New FR+EN flat keys (`calendar.view.agenda`/`…agendaLabel`, `calendar.agenda.event.label`, `calendar.agenda.nowLabel`); the day-header + time strings come from the **formatter (locale data), not catalog keys**. The agenda branch needs **no calendar-kit mock** (a plain `SectionList`) — CI proves the two helpers (90%) + the screen's events→sections→tiles wiring; the dense visual correctness + frame rate fold into the **existing** calendar on-device pass (`inbox/2026-06-16-calendar-visual-brand-review.md` extended to name the agenda — no new note). `.maestro/calendar.yaml` extended (switch to agenda → assert a fixture event). **Observability ➖ N/A** (read-only — D6). **No new ADR** (D5 — the load-bearing call is ADR [019](./decisions/019-calendar-rendering-adopt-calendar-kit.md); the SectionList-over-FlashList + date-fns-display-only choices are `design.md` decisions), **no `decisions/README.md` change, no lint-rule change** (rides existing feature-boundary B-1..B-4 / no-hardcoded-strings / a11y / import-order / coverage gates). Landed green (tsc + lint + 239 tests + coverage). *Why:* Phase-04 item 1b — completes the day/week/**agenda** rendering surface per ADR 019. → [calendar.md](./calendar.md), [features.md](./features.md); ADR [019](./decisions/019-calendar-rendering-adopt-calendar-kit.md).

- **2026-06-16 · `add-mobile-calendar-sync`** (Phase-04 item 3 — sync + offline cache) — The calendar renders **real synced data**: the deliberately-source-agnostic events-source seam (`useCalendarEvents(range)`, designed to absorb a source swap) was **swapped** behind its unchanged signature + the unchanged `CalendarEvent` shape, so the day/week timeline and the agenda are untouched. The **third real Drizzle table** `calendarEvents` (SQL `calendar_events`, the second device-local sync surface) mirrors the Flutter `CalendarEvent.toDbMap()` / the server `CalendarEventForPublic` DTO **verbatim** for Phase-09 importer fidelity (the ADR 011/018 posture, a third time): `uid` TEXT PK; `title`/`color`/`groupColor` `#RRGGBB` TEXT; `startsAt`/`endsAt`/`exportedAt` UTC ISO-8601 TEXT (ADR 011/D4 — lexicographic = chronological for `findInRange`); nullable `location`/`description`; `allDay` `integer({mode:"boolean"})`; **`teachers`/`tags`/`fields` as the first non-scalar columns — plain TEXT JSON with pure DEFENSIVE mappers (the genuinely new wrinkle, ADR 021/D2): NOT Drizzle `mode:"json"`, so a corrupt/legacy value degrades to a safe default (`[]`/`null`) instead of throwing the whole list read; the rich `EventTag[]` stays in the row for importer fidelity while the domain exposes `tags: string[]` (names) and `canceled` from `fields?.canceled`**; `type` TEXT **verbatim** (NOT a checked enum — an unknown future server value must round-trip; Flutter `toDbMap()` doesn't persist `type`, so the importer supplies a safe `class` default — recorded so it's not a fidelity gap); `userCalendarId` TEXT **soft ref, NO FK** (drop+replace reconciles). The **third committed migration** (`0002_first_mauler.sql` `CREATE TABLE calendar_events`, a third `_journal.json` entry, `0002_snapshot.json`, an updated `migrations.js` importing `m0000`/`m0001`/`m0002`) — fresh-clone-no-codegen; the runner applies all three; `migrate.test.ts` unchanged + green. The `@/db` seam re-exports `calendarEvents` (no new operator — `and`/`gte`/`lte` already there for personal-events range queries). The full **`src/features/calendar/data/sync/`** sub-module: pure `rowToCalendarEvent`/`calendarEventToRow` + `fromCalendarEventDto` mappers (the only generated-DTO import, B-1); a **TRANSACTIONAL `replaceAll`** (`db.transaction(tx ⇒ delete-all then chunked bulk insert)` — atomic so a crash mid-replace never leaves a half-empty table, ADR 021/D3); `findInRange`; the reactive `useSyncedEvents(range)` (`useLiveQuery`); the `useSyncCalendars()` orchestrator (read durable `user_calendars` tokens → batch `POST /calendars/sync` over `customFetch` → flatten+map → `replaceAll`; empty tokens → no-op); and the `useStartupSync()` fire-and-forget once-effect mounted in `_layout.tsx` (through the feature `data/` hook, never `@/db` directly — B-3/B-4). **`useCalendarEvents` now merges `useSyncedEvents` + the personal-events read**; the dense-week fixture is removed from the default merge (dev/test-only). **Triggers:** startup (silent on failure — offline launch shows last-good rows) + **pull-to-refresh** on the calendar screen (a brand-tinted `RefreshControl` on the agenda `SectionList`) + an accessible **sync-error + retry** banner across all views. **Observability split (ADR 021/D6):** a recoverable **fetch** failure → `isError` only, **NOT** `recordError`'d (mirrors the read path); a crash-worthy local **`replaceAll` transaction** failure → `@/firebase` `recordError(error, "calendar/sync")` + `isError`. New FR+EN keys (`calendar.sync.refreshing`/`.refreshingLabel`/`.error`/`.retry`/`.retryLabel`). CI proves the mappers (round-trip, importer-fidelity verbatim, canonical-UTC, JSON encode/decode + **corrupt-JSON → safe default**, null/boolean), the repository query shape + the **transactional drop+replace** (delete-then-insert inside one `transaction`, chunked), the DTO mapper, the **sync wiring at the `customFetch` seam** (success / no-tokens no-op / fetch-failure→`isError` no-record / replace-failure→`recordError`), the reactive hook + the startup trigger, and a **restart-simulation** (a fresh repository module reads back a prior `replaceAll` through a stateful Map-backed `@/db` fake). On-disk SQLite survival, drop+replace **atomicity** after a mid-sync kill, real-data perf, and Crashlytics arrival are the **on-device manual pass** (`inbox/2026-06-16-calendar-sync-on-device.md`; the low-end-Android perf note scoped to dense synced data). **No new dependency, no `app.config.ts`/babel/native change** (a new SQLite table + a generated-hook consumer under the existing seams). **ADR [021](./decisions/021-calendar-event-storage-and-sync.md)** records the verbatim schema + TEXT-ISO-UTC (building on ADR 011/018), the JSON-as-TEXT-with-defensive-mappers call, the transactional drop+replace, `type`-stored-verbatim, and the isError-vs-recordError split. Landed green (tsc + lint + 274 tests + coverage; `data/sync` 100% lines/funcs, 97% branch). *Why:* Phase-04 item 3 — the calendar renders real synced data, offline-durable. → [storage.md](./storage.md), [calendar.md](./calendar.md), [features.md](./features.md); ADR [021](./decisions/021-calendar-event-storage-and-sync.md).

- **2026-06-16 · `add-mobile-calendar-sync` (review round 1 — verbatim live write)** — Closed a fidelity gap the original sync ship shipped: the **live sync writer now fills `calendar_events` VERBATIM from the DTO**, identical in fidelity to the Phase-09 importer's direct-row path. The original design routed the live write through the lossy domain (`DTO → CalendarEvent → row`), so the row mapper fabricated degenerate values (`groupColor = color`, `type = "class"`, name-only tags with empty color/icon, `fields = {canceled}` only) — contradicting ADR 021/D1's load-bearing verbatim-importer-fidelity claim on **100% of live writes** (the schema's extra columns `groupColor`/`type`/rich-`tags`/rich-`fields` exist ONLY for that fidelity). The fix: a new pure **`dtoToRow(dto, userCalendarId)`** WRITE mapper in `calendar/data/sync/types.ts` maps the `CalendarEventForPublic` DTO → a `calendar_events` row with **zero data loss** (full `groupColor`, the real `type` enum, the full `EventTag[]` `{name,color,icon}` JSON-encoded, the full `CalendarEventCustomFields` JSON-encoded, canonical-UTC dates); `replaceAll` now takes **ROWS** (the verbatim insert shape), and `sync.ts` flattens `calendars.flatMap(c => c.events.map(e => dtoToRow(e, c.calendar.id)))` straight to `replaceAll(rows)` — the lossy domain projection **never touches a write**. The domain `CalendarEvent` stays a deliberately-lossy RENDERING projection derived FROM the row via the existing `rowToCalendarEvent` (no domain widening — no view needs the rich fields). The now-dead `calendarEventToRow` (domain→row) + `fromCalendarEventDto` (DTO→domain) mappers were **removed**. `types.test.ts` now asserts groupColor/type/rich-tags(color+icon)/fields(subject) **survive DTO→row verbatim**. **Non-blocking cleanup:** the **double range-filter** was collapsed to one — `useSyncedEvents()` (no longer takes `range`) returns all mapped rows; `useCalendarEvents` filters the merged set once. **No schema change** (mapper rerouting only) — `npm run generate:migrations` is idempotent, `migrate.test.ts` still applies all 3 migrations. Landed green (tsc + lint + 274 tests + coverage; `data/sync` 100%). → [storage.md](./storage.md), [calendar.md](./calendar.md), [features.md](./features.md); ADR [021](./decisions/021-calendar-event-storage-and-sync.md) (amended — see its Status).
