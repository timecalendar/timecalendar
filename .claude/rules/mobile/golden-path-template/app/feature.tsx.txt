// TEMPLATE — copy to src/app/<feature>.tsx and drop the .txt suffix.
//
// The THIN ROUTE — a one-line re-export of the presentational screen through the
// feature's ui/ sub-barrel (route-structure rule: route screens that need a test are thin
// entrypoints over a feature module, so the colocated *.test.tsx — which lives in ui/, not
// src/app/ — stays out of the Metro route-bundling require.context).
//
// Then register it in src/app/_layout.tsx as a <Stack.Screen name="<feature>" />
// SIBLING of (tabs) (a bare sibling under the native tabs is unreachable).
//
// References: src/app/settings.tsx, src/app/personal-event-form.tsx.

export { FeatureScreen as default } from "@/features/feature/ui"
