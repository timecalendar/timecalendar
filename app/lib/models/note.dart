class Note {
  final int id;
  final String title;
  final String text;
  final DateTime createdAt;
  final DateTime updatedAt;

  Note({
    this.id,
    this.title,
    this.text,
    this.createdAt,
    this.updatedAt,
  });
}
