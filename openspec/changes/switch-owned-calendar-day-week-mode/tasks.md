## 1. Persist and validate the Calendar view

- [x] 1.1 Extend `mobile/src/features/settings/prefs/store.test.ts` first for all `day | week | agenda` round trips plus missing/corrupt fallback to week; add the namespaced key, closed type/parser, and imperative get/set functions through `@/storage`, then run the exact store suite.
- [x] 1.2 Extend `mobile/src/features/settings/prefs/hooks.test.ts` first for default week, reactive Day/Week/Agenda writes, remount/restart-shaped reads, and stable setter behavior; add/export the reactive Calendar view hook through the settings preference barrels and run the exact hook suite.
- [x] 1.3 Add the view key to `mobile/src/storage/index.ts` as environment-independent; extend `mobile/src/storage/storage.test.ts` and affected backend-reset tests to prove classification coverage and preservation beside Show weekends while backend-bound values clear, then run every edited storage/reset suite.

## 2. Generalize civil transition policy

- [ ] 2.1 Extend `mobile/src/features/calendar/data/week-transition.test.ts` first with the complete day/week normalize, page-step, and switch table across month/year and DST boundaries, repeated/idempotent replacements, cancelled/stale revisions, and Friday→Saturday→Sunday day steps with weekends hidden.
- [ ] 2.2 Replace week-only transition names/types with a mode-aware pure state and helpers that carry the committed timeline mode, normalize Day to one civil date and Week to the containing explicit-first-weekday anchor, advance by one/seven civil dates, and invalidate pending work on mode/date replacement; keep fixed-duration arithmetic, locale/device globals, React, storage, and presentation out of the module.
- [ ] 2.3 Update `mobile/src/features/calendar/data/index.ts` and affected imports/tests to the new transition contract, retain explicit Monday launch input and unchanged week helpers, make introduced logic fully branch-covered, and run the focused transition, week, and day-key suites.

## 3. Reuse the owned renderer for one-day geometry

- [ ] 3.1 Extend `mobile/src/features/calendar/renderer/owned-calendar-shell.test.tsx` first for one-column Day pages, five/seven-column Week pages, exactly three direct pages in both modes, shared header/grid keys, weekend-visible Day behavior, mode-specific labels, and the retained native scroll/pager/automatic-inset structure.
- [ ] 3.2 Pass the explicit timeline mode through `OwnedCalendarShell` and its coordinator; derive previous/current/next page anchors and columns from the pure mode policy while keeping the existing passive header and canvas, one `ScrollView`, one animated `PagerView`, one UI-thread progress source, and no renderer/storage/navigation dependency crossing.
- [ ] 3.3 Treat a mode change as a generation replacement in the coordinator: cancel its pending revision, reject callbacks captured by the old generation, reset selected page and header progress to center, recenter without animation, and preserve/restore the settled raw vertical offset through the existing native owner.
- [ ] 3.4 Add renderer regressions for switching during partial forward/backward drag, late selected/idle/accessible callbacks, AppState inactivity, reduced motion, lane-size and weekend changes, accepted paging, and repeated Day/Week switches; prove no stale commit or duplicate announcement path and run the exact renderer suite.

## 4. Commit view, date, chrome, and accessibility together

- [ ] 4.1 Extend Calendar controller tests first for persisted view initialization, fresh Day=today and Week=containing launch week, Day↔Week mapping, retained Agenda anchor behavior, settled vertical-offset preservation, Today/focusDate mode coherence, and monotonic cancellation/generation state; then wire the preference and transition reducer through one controller change path.
- [ ] 4.2 Add typed-parity French and English Day plus previous/next-day strings, keeping the existing Week/Agenda and previous/next-week strings; run i18n key-parity/type checks after the catalog edit.
- [ ] 4.3 Extend `calendar-view-menu`/Calendar screen tests first for Day, Week, Agenda order and selected state on iOS and Android, persistent menu changes, one-column versus week renderer props/headings, mode-correct accessible actions, one accepted-settlement announcement, and no announcement from mode replacement or stale callbacks; implement both platform menus and screen projection, then run those suites.
- [ ] 4.4 Re-run retained Calendar regressions for Week/Agenda switching, seven-day Agenda range, refresh/retry, checklist progress, synced/personal event-details routing and return, Add, device clock preference, Show weekends, Today, one-shot `focusDate`, remount, and accepted/cancelled page settlement without claiming T08, T17, or T18 behavior.

## 5. Update executable contracts and current architecture

- [ ] 5.1 Update `mobile/calendar-owned-shell.contract.test.ts` as the CI proof test for typed environment-independent mode persistence, mode-aware civil stepping, one/five/seven page geometry, one native vertical owner, one animated pager, three-page retention, synchronized header projection, automatic insets, and absence of vendor/fallback/compatibility/second renderers or motion owners; retain the exact three Maestro journeys and Agenda helper, then run the contract suite.
- [ ] 5.2 If the existing deterministic personal-event Maestro journey can exercise Day↔Week without changing its purpose, add the bounded interaction and update selector/harness contracts; otherwise record why focused automation plus owner QA is the safer proof. Do not add a fourth top-level journey, retries, or weakened selectors.
- [ ] 5.3 Update `docs/mobile/architecture-book/calendar.md`, `storage.md`, `testing.md`, `features.md`, and `CHANGELOG.md` with the current T05 mode/date/persistence/ownership contract and explicit T08/T17/T18 boundaries. Add no ADR unless implementation evidence changes an approved costly-to-reverse decision.
- [ ] 5.4 Create or update a `(HUMAN: owner device verification)` inbox note with exact revision/build, fabricated empty-week setup, Agenda-transfer limitation, device/OS/build fields, and the complete T05 owner checklist: afternoon offset, Week→Day Monday, moved Day→containing Week, repeated-switch coherence, restart persistence/fresh date, partial-drag cancellation, weekend-hidden Saturday/Sunday paging, and touched prior interactions.
- [ ] 5.5 Re-read the implemented delta against P01/P02, D01/D02, the canonical T05 ticket, delivery protocol, mobile Architecture Book, and current Expo/React Native seams; stop and re-brief before widening Agenda/Today/current-time scope, changing an approved boundary, adding a dependency/route/renderer/gesture owner, or touching a sensitive surface.

## 6. Produce local-green, CI, and owner evidence

- [ ] 6.1 Run `openspec validate switch-owned-calendar-day-week-mode --strict`; repair proposal, design, specs, and tasks together if implementation evidence changes the feature shape, and never weaken a requirement to fit an implementation.
- [ ] 6.2 From `mobile/`, run every edited suite with exact `npm test -- --runTestsByPath ...` paths, including transition/week/day-key, settings store/hooks, storage/reset, renderer, controller/Calendar screen, platform menu/i18n, repository contract, and any edited Maestro selector/harness tests; record commands, suite/test counts, results, and tested Git revision.
- [ ] 6.3 Run focused coverage for introduced pure transition/parser logic while preserving project thresholds, then run `npx tsc --noEmit`, `npm run lint`, scoped `npx prettier --check <edited-files>`, `npm run react-doctor:changed`, `git diff --check`, and any established Maestro harness contracts affected by the change.
- [ ] 6.4 Audit the final diff for display-zone civil arithmetic, explicit first weekday, one versus five/seven columns, day weekend traversal, atomic mode/date generation, preserved clock offset, mode-specific semantics, typed reset-safe persistence, Agenda/details retention, one renderer/scroll/pager, three pages, automatic insets, and no changes under API/generated client, server migrations, native/store config, deployment/CI, or legacy Flutter paths.
- [ ] 6.5 Push the exact tested implementation head to this same draft PR; update and re-read its feature-level body with exact local/native evidence, sensitive-surface status, T05 limitations, build/fixture instructions, and remaining owner checks after running the disclosure scan on the exact text.
- [ ] 6.6 Confirm the PR head equals the revision used by the reported standard CI run and every required check is green; treat the pure transition/persistence suites and `mobile/calendar-owned-shell.contract.test.ts` as direct CI proof for the new contract.
- [ ] 6.7 Supply the exact build/revision and canonical T05 checklist for explicit owner QA, address failures and focused retest evidence on this same ticket, and do not make T06 eligible until owner acceptance and the merged revision are recorded.
