## 1. Replace automatic native triggers with a preparation controller

- [x] 1.1 Update `.github/workflows/ci-mobile-e2e.yml` so `on` contains exactly one daily `schedule` plus `workflow_dispatch` with a required non-empty `ref` input; remove `push`, `pull_request`, `production`, `run-e2e`, and every label-based job condition. Verify the focused structure test rejects each removed trigger shape.
- [x] 1.2 Add least-privilege `contents: read` and `actions: read` permissions plus one workflow-level concurrency group shared by scheduled and manual runs with cancellation disabled. Verify both policy fields structurally and confirm no deploy/write permission is introduced.
- [x] 1.3 Add a lightweight preparation job that checks out full history, resolves scheduled `main` or the manually supplied ref once to `target_sha`, and emits `should_run` plus comparison diagnostics. Pass manual ref text through action inputs/environment rather than shell interpolation, fail an invalid ref before native allocation, and verify all outputs in the focused test.
- [x] 1.4 For scheduled events, query this workflow's preceding scheduled run by run number, use its `head_sha` regardless of conclusion, run both platforms when no predecessor exists, and otherwise diff that SHA to current `main`. Verify the structure test covers first-run fallback, previous-attempt rather than fixed-time/successful-run lookup, visible skip/run reasons, and failure on an unusable comparison rather than a silent skip.
- [x] 1.5 Define and test the relevant path set from the live workflow/lifecycle dependencies: `mobile/**`, `openapi/**`, `server/**`, `ci/e2e-server.sh`, `ci/generate-dummy-firebase-key.sh`, `.nvmrc`, and `.github/workflows/ci-mobile-e2e.yml`; add any other directly read E2E lifecycle/config path found during implementation and document why.

## 2. Fan out one immutable selected revision to both platforms

- [x] 2.1 Make `build-server`, `e2e-mobile-android`, and `e2e-mobile-ios` depend on preparation and its `should_run` output, and make every checkout plus server-image tag use `target_sha`. Verify no downstream job resolves a mutable branch or uses `github.sha` as the application/server target.
- [x] 2.2 Preserve the complete Android and iOS native job contracts: release-config development builds, platform-correct local URLs, pinned Maestro, per-flow harness, Android resource bounds, iOS startup-attempt boundary, and one shared backend lifecycle. Run existing focused harness tests to prove no flow behavior changed.
- [x] 2.3 Preserve job failure semantics and all four evidence artifacts (`maestro-debug-android`, `maestro-debug-ios`, and both platform server-log artifacts). Extend the workflow test so removing or weakening an upload-on-failure contract is red.
- [x] 2.4 Update `.github/workflows/ci-mobile.yml` comments/path filters or invocations as needed so ordinary pull requests continue to run `mobile/e2e/test_run_e2e.sh` and `mobile/e2e/test_ci_mobile_e2e.sh` when the native workflow or focused proof changes, without invoking the native workflow.

## 3. Strengthen emulator-free workflow proof

- [x] 3.1 Refactor `mobile/e2e/test_ci_mobile_e2e.sh` only as far as needed to inspect trigger/job/step ownership reliably, retaining existing build-input and toolchain assertions. Add focused assertions for the schedule/manual-only trigger contract, required manual ref, read-only permissions, shared non-cancelling concurrency, and absence of the retired label/push/PR model.
- [x] 3.2 Add focused assertions for scheduled change detection: previous scheduled run selection, conclusion-independent `head_sha` boundary, full-history comparison, first-attempt run, explicit relevant categories, and no fixed 24-hour or last-success fallback. Prove representative mutations of each structural branch make the test fail without running GitHub Actions or native devices.
- [x] 3.3 Add focused assertions that manual dispatch always selects native execution, preparation resolves/logs one SHA, all three downstream jobs share its condition/target, and both Android and iOS remain in the graph. Run the test against at least one mutation that drops each platform or bypasses preparation.
- [x] 3.4 Run `bash -n mobile/e2e/test_ci_mobile_e2e.sh mobile/e2e/test_run_e2e.sh mobile/e2e/run_e2e.sh` and execute `./mobile/e2e/test_ci_mobile_e2e.sh` plus `./mobile/e2e/test_run_e2e.sh`; run ShellCheck on touched shell when available and record an explicit N/A otherwise.

## 4. Reconcile the binding testing policy

- [x] 4.1 Re-check the ADR index and open PR reservations, then add the next safe ADR with `## Decision` sections for the daily/manual health-signal policy, previous-scheduled-attempt SHA boundary, both-platform cadence, immutable manual target, baseline/native separation, concurrency, alternatives, consequences, and a concrete revisit condition; index it and record the rule change in the Architecture Book changelog.
- [x] 4.2 Update `docs/mobile/architecture-book/testing.md` and `definition-of-done.md` so committing a meaningful cross-platform Maestro flow remains applicable feature work while native execution is scheduled/manual health evidence, not ordinary merge proof. Keep selector, harness, unit/component/integration/type/lint checks in the baseline.
- [x] 4.3 Update Phase 01 and Phase 10 roadmap wording plus `00-exploration/migration-approach.md` where it defines the DoD: both-platform E2E health remains required, but no per-feature/per-commit native run is required and relevant daily/manual evidence owns runtime health.
- [x] 4.4 Update `docs/agent-dev-environment.md`, `mobile/e2e/README.md`, and active ship-loop/agent-facing repository guidance to document trigger cadence, relevant paths, manual ref/SHA dispatch, skip diagnostics, concurrency, and failure-artifact behavior. Search active `.github`, `docs`, `mobile`, and `openspec/specs` content for `run-e2e`, label-gated native jobs, per-push native execution, exact-head native proof, and unconditional "Maestro runs on main" claims; reconcile current guidance without rewriting archived OpenSpec history.
- [x] 4.5 Reconcile the canonical `openspec/specs/mobile-e2e/spec.md` through this delta/archive so it contains the conditional health-signal and previous-attempt requirements, retains both-platform/build/artifact behavior, and no longer requires terminal `main` or exact-reviewed-head native proof.
- [x] 4.6 Confirm no human-only credentials, device install, console registration, production action, or other new operator prerequisite was introduced; therefore do not create a migration inbox note unless implementation reveals a genuinely new human-only step.

## 5. Local-green verification and CI proof

- [x] 5.1 Parse/lint `.github/workflows/ci-mobile-e2e.yml` with the repository's available YAML/action tooling and run the focused mutation-backed workflow tests; confirm the proof requires no emulator, simulator, KVM, live credential, or GitHub API mutation.
- [x] 5.2 Run `openspec validate make-native-e2e-daily-health-check`, repository formatting for every touched YAML/Markdown/shell file, and `git diff --check`. Review the complete diff for only the workflow, focused tests, binding docs/ADR, and OpenSpec change; confirm no OpenAPI/generated code, migrations, native/store/EAS/Firebase config, infrastructure, deploy path, secret, or legacy Flutter change.
- [ ] 5.3 Use the ordinary exact-head baseline CI as the PR proof test: confirm its mobile job executes the static Maestro selector, E2E harness, and workflow-structure checks while the native E2E workflow is absent from pull-request checks. Do not add a label or otherwise recreate native merge gating.
- [ ] 5.4 For deliberate native evidence on this E2E-focused change, manually dispatch the implementation commit SHA and record preparation's resolved SHA plus both platform results and retained failure artifacts if either fails. Treat the run as diagnostic evidence: a runner failure does not block unrelated delivery or justify retry-loop expansion.
- [ ] 5.5 Run the repository disclosure preflight over the finished branch and exact PR title/body before any PR update, then update task checkboxes and current-state documentation to match the implementation that actually landed.
