## 1. Lock the current contracts and native API shape

- [ ] 1.1 Extend the existing calendar-management and rename test harnesses with explicit iOS/Android fixtures for list top-spacing ownership, menu image sources, native text-buffer changes, pending/error rerenders, outside dismissal, hardware Back, and late completion after cancel; verify the focused suites fail for the missing geometry, icon, and native-wrapper behavior before production edits.
- [ ] 1.2 Inspect the installed Expo SDK 56 `@expo/ui/swift-ui`, `@expo/ui/swift-ui/modifiers`, `@expo/ui/jetpack-compose`, and `@expo/ui/jetpack-compose/modifiers` typings and implementations; record the exact supported Host, observable state, text field, button, progress, alert-dialog, accessibility-id, disabled, keyboard-inset, and dialog-property APIs in implementation comments where a caveat is not type-encoded.
- [ ] 1.3 Select a monochrome Android `drive_file_rename_outline` Material Symbols source compatible with `MenuView`'s `ImageSourcePropType`, add only the required density/source asset plus license attribution if the source requires it, and verify Metro resolves the asset without a dependency, Babel plugin, app config, or native-resource edit.

## 2. Build the controlled native text-entry chrome seam

- [ ] 2.1 Add a product-neutral `NativeTextEntryDialog` contract under `mobile/src/components/chrome/` with title, initial buffer, label/placeholder, inline message, cancel/submit labels, pending/disabled state, stable ids, change, submit-current-value, and cancel callbacks; export only that stable contract through the chrome barrel and keep every platform-specific Expo UI import in the chrome directory.
- [ ] 2.2 Implement the iOS renderer as a controlled over-full-screen modal with background focus isolation and inert backdrop, keyboard-avoiding compact-width ownership, and one SwiftUI Host composing native title/message text, text field, progress, and Cancel/Save buttons; assign the existing `user-calendar-rename-*` selectors through native accessibility identifiers and verify no SwiftUI `Alert`/`Alert.prompt` auto-dismiss path is used.
- [ ] 2.3 Implement the Android renderer with a Compose Host and Material 3 `AlertDialog`, `OutlinedTextField`, progress, and native text buttons; set outside-click dismissal false, map enabled hardware Back dismissal to explicit cancel, retain platform width/IME inset handling, and attach the same stable selectors through Compose test-id modifiers.
- [ ] 2.4 Create the native observable text state once per mounted dialog, forward changes for validation, and submit the current native buffer rather than a possibly delayed React snapshot; do not apply `maxLength`, do not bind to a reactive calendar row, and verify pending/error prop changes preserve the complete fast-typed draft.
- [ ] 2.5 Extend `mobile/jest/setup-expo-ui.ts` with suite-wide faithful mocks for each newly imported platform subpath and modifier module, retaining children, callbacks, observable-buffer behavior, disabled/busy props, dialog properties, and test identifiers; verify unrelated existing `Picker`, `DateTimePicker`, and `MenuView` suites remain green.
- [ ] 2.6 Add focused chrome tests for iOS versus Android native primitive selection, current-buffer submission, compact/rotation layout ownership, inline message and native busy state, iOS modal isolation/inert backdrop, Android outside-tap policy/hardware-Back cancel, stable selectors, and keyboard-reachability structure without claiming device rendering from Jest.

## 3. Route rename orchestration through the native seam

- [ ] 3.1 Refactor `RenameCalendarDialog` into feature-owned orchestration over `NativeTextEntryDialog`: seed `trim(calendar.name)` once, preserve the localized placeholder/title/labels, derive over-100-trimmed and mutation error messages, and keep empty/whitespace drafts legal; remove the app-styled `Modal`, `TextInput`, card, and `Pressable` actions from the feature.
- [ ] 3.2 Recheck the length predicate inside submit before invoking `useRenameCalendar`, block repeat submission while pending, retain the draft and inline error after rejection, and make Retry submit the same current native buffer; verify no failed request closes the dialog or changes the old local name.
- [ ] 3.3 Gate the success announcement and close on resolved server plus local persistence, preserve existing effective-name fallback copy, and suppress a second close or success announcement if explicit cancel unmounts the dialog while a request is pending; verify the data hook, cache update, selector, and observability behavior remain unchanged.
- [ ] 3.4 Update focused rename tests to drive the chrome contract rather than React Native host controls and prove 101-character feedback without truncation, exactly 100 and empty success, pending single-flight state, failed draft retention, retry, Cancel/Back/outside behavior, success-only announcement, and stable test ids on both platform renderers.

## 4. Refine calendar-management scrolling and menu iconography

- [ ] 4.1 Remove only calendar management's outer `RootPage` top padding and add `Spacing.four` to the populated `FlatList` content so `PageIntro` starts at the shared first-content inset at offset zero and that inset scrolls away; retain one standard measured lane and one virtualized list with unchanged id keys, row gap, and header ownership.
- [ ] 4.2 Give visible non-list siblings the appropriate shared top rhythm after the route-local zero-top override: preserve the unresolved blank branch, centered loaded-empty state, and accessible write-error notice without adding a scroll owner, top safe-area inset, sticky spacer, negative margin, or global `RootPage` change.
- [ ] 4.3 Preserve horizontal/bottom safe areas, tablet centering, ordinary list bottom padding, Android FAB clearance, iOS native header add action, Android FAB placement, and conditional rename mounting; verify concrete styles and ownership for loading, empty, populated, error, compact, tablet, iOS, and Android cases.
- [ ] 4.4 Add `pencil` as the iOS Rename SF Symbol and the bundled `drive_file_rename_outline` Material Symbols image source on Android while keeping the `rename` id, localized title, accessibility meaning, Delete action/destructive attribute, menu trigger, TalkBack `show()` path, and row selectors unchanged.
- [ ] 4.5 Add a focused scroll-geometry CI proof that fails if top spacing returns outside the list, initial content loses the shared inset, a second scroller appears, tablet/safe-area geometry drifts, or Android bottom/FAB clearance regresses; add platform menu assertions that fail if either Rename icon or its unchanged label/id is missing.

## 5. Align the Architecture Book and OpenSpec

- [ ] 5.1 Add a load-bearing ADR for the narrow composed-chrome exception: a stable cross-platform behavior contract may privately compose divergent native alpha APIs, while feature state/data stays outside and ordinary native controls remain thin re-exports; include rejected SwiftUI Alert, raw platform exports, feature-specific wrapper, and universal custom-card alternatives plus revisit triggers.
- [ ] 5.2 Update `docs/mobile/architecture-book/theming.md`, `accessibility.md`, `testing.md`, and the calendar-sources current-state entry in `features.md` for the native dialog seam, observable buffer ownership, explicit dismissal/focus/keyboard rules, platform menu icon sources, list-owned top rhythm, and the split between automated structure proofs and device evidence.
- [ ] 5.3 Append the Architecture Book `CHANGELOG.md` entry and add a dated `docs/react-native-migration/inbox/` note tagged `(HUMAN: …)` for iOS/Android keyboard-open reachability, rotation/compact width, Dynamic Type, light/dark appearance, VoiceOver/TalkBack isolation, Android Back, outside tap, native menu icons, and scrolled header clipping; keep the note non-blocking and describe current state rather than an incident.
- [ ] 5.4 Reconcile the `mobile-user-calendars` delta during archive without weakening the existing rename data, delete, visibility, cache, localization, selector, and observability requirements; validate that the old single React Native Modal/TextInput/Pressable requirement is fully replaced by the controlled native composition contract.

## 6. Focused and local-green verification

- [ ] 6.1 Run focused Jest for `src/components/chrome/`, `src/features/calendar-sources/ui/rename-calendar-dialog`, `src/features/calendar-sources/ui/calendar-row`, and `src/features/calendar-sources/ui/user-calendars-screen` with natural timeouts; verify controlled buffers/promises are used without retries, extended waits, snapshots in place of behavior, or coverage exclusions.
- [ ] 6.2 Run the existing calendar rename data-layer tests to confirm trimmed request input, server-response persistence, local-write failure recording, cache behavior, effective-name fallbacks, and sync semantics remain unchanged.
- [ ] 6.3 Run `npx tsc --noEmit`, `npm run lint`, and React Doctor against the changed mobile files; fix typed platform props, import-boundary, accessibility, i18n, and compiler findings without suppressions, and classify any remaining React Doctor output with evidence.
- [ ] 6.4 Run `npm test -- --coverage` from `mobile/` and verify the normal 90% logic/70% global thresholds, suite-wide native mocks, and unrelated chrome consumers remain green.
- [ ] 6.5 Run `openspec validate refine-mobile-calendar-management-native-rename --strict`, `git diff --check`, and a complete branch scope/disclosure review; confirm no drift in `openapi/openapi.json`, `mobile/src/api/generated/`, `server/src/migrations/`, `mobile/app.config.ts`, `mobile/eas.json`, `mobile/firebase/`, deploy/CI config, secrets, or legacy `app/`, and confirm no dependency was added.

## 7. CI proof and Definition of Done

- [ ] 7.1 Push the implementation and verify the normal mobile CI job at the exact branch head passes generated-client drift, Expo type generation, TypeScript, lint, Jest coverage, commit identity, and disclosure gates; report path-skipped jobs as skipped rather than green and do not add the normally-disabled native E2E label solely for this change.
- [ ] 7.2 Treat the focused native-dialog contract tests, scroll-spacing ownership test, and platform menu-action test as the required CI proof: demonstrate they fail if native controls are replaced by app-styled inputs/buttons, failed input is lost, submit auto-dismisses, outside tap dismisses, Back cannot cancel, spacing stays outside the list, or either platform icon disappears.
- [ ] 7.3 Walk the mobile Definition of Done for architecture, boundaries/types, tests, performance/virtualization, accessibility, i18n, native behavior, observability, documentation, and rollback; record the host's inability to render devices honestly and link the non-blocking `(HUMAN: …)` evidence note rather than presenting Jest as keyboard/screen-reader proof.
- [ ] 7.4 Before review handoff, rerun workspace and disclosure preflights and inspect the complete diff; flag `docs/mobile/architecture-book/` plus its ADR/changelog as the only sensitive surface changed and confirm API/generated, schema/migrations, native/store/EAS/Firebase, deployment/CI, and legacy Flutter surfaces remain untouched.
