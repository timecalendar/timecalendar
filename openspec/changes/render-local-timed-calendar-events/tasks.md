## 1. Define and prove the validated local-event boundary

- [ ] 1.1 Replace the permissive `CalendarEvent` contract with schema-versioned `TimedCalendarEventV1` / `DateOnlyCalendarEventV1` types carrying stable synced/personal UID identity, validated interval/date fields, presentation fields, and source metadata; migrate the calendar data barrel and compile-time fixtures coherently. Verify the discriminant, complete fields, and original identity in `mobile/src/features/calendar/data/types.test.ts` (create it if needed).
- [ ] 1.2 Implement pure total synced/personal row decoders that validate required dates, identity, range order, date-only civil keys, colors, optional strings, teachers/tags/fields, and exact cancellation before any map/sort/format operation; return accepted values plus closed aggregate rejection counts without throwing or mutating rows. Extend `sync/types.test.ts` and personal/domain tests for valid timed/date-only rows, corrupt dates/JSON/elements, non-positive ranges, and valid-sibling isolation; reach 100% statement and branch coverage for introduced decoder logic.
- [ ] 1.3 Add a pure support classifier for T09's positive-duration, one-display-zone-date, no-offset-transition timed shape while retaining explicit tagged values for date-only, instant, spanning, and DST-deferred cases. Cover boundary midnight, exact exclusive end, spring/fall offset changes, invalid zones, and stable support/rejection reasons at 100% statement and branch coverage.

## 2. Bound local SQLite reads to the three-page range

- [ ] 2.1 Add a pure three-page range planner that derives ordered previous/current/next Day/Week page and column identities, one half-open instant envelope, and one floating civil-date envelope from the settled anchor/mode/zone/first-weekday/weekend policy. Prove day/week strides, hidden-weekend columns, month/year/offset crossings, exact boundaries, and exactly-three retention in a focused data suite with 100% statement and branch coverage.
- [ ] 2.2 Narrowly export only the additional Drizzle predicates required by the repositories from `@/db`, then add range-scoped reactive synced timed/date-only and personal timed reads. Use SQL half-open intersection with no `LIMIT`, no whole-table pre-read, and no schema/index change. Prove query shapes and exact boundary inclusion/exclusion with `createFakeDb` in the synced/personal repository or hook suites.
- [ ] 2.3 Rebuild `useCalendarEvents(range)` on the bounded repositories and tagged decoder; apply invisible/deleted source, hidden UID/name, and exact-cancelled filtering before every Home/Agenda projection, isolate bad rows, and keep personal events independent of calendar visibility. Migrate affected Home/Agenda selectors, fixtures, and suites without changing user-visible behavior; test cancellation, malformed siblings, date-only grouping, visibility restore, and range changes.
- [ ] 2.4 Emit malformed-row diagnostics at most once per completed snapshot revision using only a static context, allowlisted reason, and aggregate count. Add focused tests proving no raw exception, UID, title, location, timestamp, calendar/source identity, or query value is recorded; prove ordinary filtering and same-revision rerenders emit nothing extra.

## 3. Publish complete versioned page models

- [ ] 3.1 Add pure immutable builders for `CalendarTimelinePresentationV1`, its exactly three page models, ordered columns, and chronologically sorted supported timed tiles. Include original identity, start/end minute inputs, safe surface-color input, title/location, formatted-label inputs, and stable start/end/source/UID tie-breaks; prove model completeness, immutability/copying, date assignment, deterministic ordering, and unsupported-shape exclusion at 100% statement and branch coverage.
- [ ] 3.2 Add `useCalendarTimelinePresentation` to combine the planned range, local read readiness, visible/hidden/cancelled filters, renderer generation, and checklist progress. Retain the last complete presentation while a replacement resolves, discard stale out-of-order completions, release superseded pages, and never relabel retained events. Test empty/loaded/replacement/data-removal paths, rapid range changes, stale completion, and bounded retained generation counts.
- [ ] 3.3 Collect checklist progress from one normalized scoped UID set derived only from the complete filtered three-page presentation, then project zero/partial/complete summaries onto tiles without changing event identity. Extend event-checklist progress and presentation-hook tests for mutation updates, stale/filtered UID removal, and zero-item omission.
- [ ] 3.4 Record fabricated ordinary-data evidence for the three-page range: queried synced/date-only/personal rows, accepted/rejected/filter counts, retained pages/events/replacement generations, and deterministic local preparation timing. Keep the fixture synthetic and the output aggregate-only; explicitly label it non-representative rather than claiming p50/p95 workload support.

## 4. Render and activate ordinary timed tiles

- [ ] 4.1 Extend the owned renderer facade and coordinator to accept the complete V1 presentation and an original-identity activation callback; remove duplicate page/column construction from the renderer while preserving one vertical owner, one three-page pager, current-time behavior, revision settlement, resize, and zoom contracts. Update `owned-calendar-shell.test.tsx` for complete model replacement, exact page identity, and unchanged native ownership.
- [ ] 4.2 Add a feature-private timed tile that uses prevalidated minute inputs with worklet-safe live-scale top/height projection, shows title plus optional location and existing checklist progress, and uses the shared/extracted safe event surface-color rule. Test 10:00–11:00 placement at min/default/max zoom, missing location, progress states, deterministic full-column ordering, and that zoom frames perform no database read, sorting, formatting, or React state write.
- [ ] 4.3 Replace the pager-wide accessibility exclusion with explicit decorative-grid and neighbour-page hiding while exposing only committed-page supported tiles as chronological buttons. Compose one localized title/time/location/progress label and existing details hint per tile, hide child visual semantics, retain off-viewport committed tiles, add EN/FR typed keys, and test exactly-one label plus absence of hidden/cancelled/invalid/unsupported/invisible targets.
- [ ] 4.4 Route tile presses from `CalendarScreen` through `eventRoute` using the original UID, and preserve cancellation when scroll/page/pinch owns the gesture. Extend renderer/screen tests for synced and personal activation, no press during movement, stable identity across recycled pages, and no page-slot/date/index identity leakage.

## 5. Preserve details and consumer behavior

- [ ] 5.1 Extend Calendar screen integration tests for the fabricated `Maths`, `B12`, 10:00–11:00 synced class and one personal event: both render on the intended date; the synced UID opens current read-only details; the personal UID opens the same details screen with Edit still available; returning preserves Calendar usability.
- [ ] 5.2 Add a page-away/page-back offline test that settles the owned pager, changes only bounded local range/presentation, restores the class on the right date, and proves the sync callback, generated API seam, fetch mutator, network loader, writes, and event-details reads are not invoked by navigation.
- [ ] 5.3 Run the affected Home, Agenda, event-details, synced-data, personal-data, hidden-events, calendar-source visibility, event-checklist, Calendar screen, and renderer suites after the tagged-domain signature migration; preserve their current all-day/timed grouping, activation, read-only/editable authority, refresh/error behavior, and checklist labels.

## 6. Strengthen executable repository contracts and CI proof

- [ ] 6.1 Extend `mobile/calendar-owned-shell.contract.test.ts` as the CI proof: pin the new bounded data/presentation modules and renderer inventory, exactly three complete pages, one pager/vertical owner, no compatibility renderer, no generated API/fetch/sync import below the page-navigation boundary, no per-frame event work/React writes, and original-identity rather than slot/index activation. Keep the T06–T08 worklet/timer/animation contracts green.
- [ ] 6.2 Add randomized-order and malformed-row regression coverage so test isolation cannot hide a bad decoder, stale generation, repeated diagnostic, or leaked accessibility node; use the shared fake database and supported Reanimated/RNTL harnesses with exception-safe cleanup.
- [ ] 6.3 Keep the established three top-level Maestro journeys unchanged. Add or adjust only an existing helper/journey assertion if it can prove the populated Calendar-to-details path without creating a fourth journey; otherwise record the focused unit/component/contract proof and leave the device owner checklist as the user-visible gate. Run the selector and harness inventory checks for any YAML change.

## 7. Reconcile the Architecture Book and canonical evidence

- [ ] 7.1 Update `docs/mobile/architecture-book/calendar.md`, `data.md`, `storage.md`, and `testing.md` with the bounded instant/civil-date reads, V1 tagged domain, row isolation/aggregate diagnostics, shared filters, complete page models, live-scale ordinary tiles, committed-page accessibility, checklist progress, original-identity activation, and local-only navigation. Remove only the now-superseded empty-event wording, retain T10–T16/T19–T23 limits, and append a dated `CHANGELOG.md` entry.
- [ ] 7.2 Add or revise an ADR only if implementation displaces an indexed load-bearing decision; otherwise record in review that approved D01/D02/D05/D06 and ADR 033 remain sufficient. Do not change the API contract, migrations, native/store/EAS/Firebase config, deploy/CI, legacy Flutter, or infrastructure without stopping for Founding Engineer review of the sensitive-surface expansion.
- [ ] 7.3 Update the canonical T09 execution-evidence section with only the exact tested runtime revision/build, fabricated seed/reset instructions, aggregate measurements, exact command outcomes, host/device limitations, and the complete unchecked owner checklist. Create a `(HUMAN: …)` inbox note only for a genuinely credential/device-install step; do not translate pending owner QA into a pass or defer it without explicit authorization.

## 8. Get local green and prepare the mandatory owner gate

- [ ] 8.1 Run every edited Jest suite by explicit path, including the domain/decoder/range/repository/presentation/checklist/renderer/Calendar/Home/Agenda/details/i18n/repository-contract files, then run scoped coverage over every introduced pure calendar module and confirm 100% statement/branch coverage there plus the repository's 90% logic threshold. Record exact commands, suite/test counts, and coverage results.
- [ ] 8.2 From `mobile/`, run `npx tsc --noEmit`, `npm run lint`, scoped Prettier checks over every changed source/test/JSON/Markdown file, `npm run react-doctor:changed`, and `npm test -- --coverage`; run applicable Maestro selector/harness static checks and `openspec validate render-local-timed-calendar-events --strict`. Fix failures without weakening lint, tests, coverage, timeouts, or existing contracts.
- [ ] 8.3 Push the exact implementation head and wait for green CI on that same head. Supply the testable build plus complete owner preparation: seed Monday 2026-09-14 10:00–11:00 Europe/Paris `Maths`/`B12`, one editable personal event, and one hidden/cancelled event on a disposable offline installation; name the reset/launch steps, device/build identity, and every command result.
- [ ] 8.4 Pause on this issue for the human owner to execute and verdict every canonical row: actual-time title/location, synced read-only plus personal editable details, offline page-away/return, hidden/cancelled visual and semantic absence, one complete screen-reader label, and touched prior interactions. Resolve feedback on this branch, rerun affected checks, and record explicit acceptance against the exact accepted head before Reviewer merge; never infer acceptance from silence or agent-only verification.
