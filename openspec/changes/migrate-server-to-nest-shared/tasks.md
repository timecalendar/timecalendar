# Tasks

> All tasks are in this (`timecalendar/`) repo except those tagged `[platform]`, which
> land in the platform/k8s repo. The change ships as **5 PRs ordered by risk** (design
> D7); each is independently revertable. The deploy-side env rename (PR1's `[platform]`
> task) must land **atomically** with the server image that reads the new vars.

## PR / phase breakdown

| PR | Title | Sections | Depends on |
| --- | --- | --- | --- |
| **PR1** | Converge config on `DATABASE_URL` / `REDIS_URL` (no nest-shared) | 1 | — (foundation) |
| **PR2** | Adopt `SharedDatabaseModule` (runtime + test harness) | 2 | PR1 deployed/green |
| **PR3** | Adopt `SharedRedisModule` + `SharedBullModule` | 3 | PR2 |
| **PR4** | Adopt `TypeOrmExceptionFilter` + `SharedHealthModule` + nest-shared ESLint | 4 | PR2 |
| **PR5** | (Optional, evaluated) Adopt `SharedQueueModule` | 5 | PR3 |

Across every PR: `openapi/openapi.json` MUST stay byte-identical (the infra-only
guardrail — design D8). Run the local gates in `server/` before each PR: `npm run build`,
`npm run lint`, `npm test`, and the OpenAPI drift check.

## 1. Config convergence — `DATABASE_URL` / `REDIS_URL` `[PR1]`

- [x] 1.1 In `config/constants.ts` + `config/environments/{development,test}.ts`, introduce `DATABASE_URL` (Postgres connection string) and standardize on `REDIS_URL`; derive any still-needed pieces (e.g. queue name, key prefix) from dedicated vars, not the connection string. _(Done: `constants.ts` exports a single `DATABASE_URL` (+ `DATABASE_LOGGING`); dev → `postgres://postgres@localhost:37291/timecalendar`, test → `…/timecalendar_test`. `REDIS_URL` was already URL-based — no change needed.)_
- [x] 1.2 Update `data-source.ts` (the TypeORM CLI/migration `DataSource`) to parse `DATABASE_URL` via TypeORM's `url` option — one connection-string source of truth for runtime and CLI. _(Done: `dataSourceOptions` now uses `url: DATABASE_URL`; entities/migrations/logging unchanged. Inherited by the test module, seed script, and the migration CLI.)_
- [x] 1.3 Remove the split DB vars (`DATABASE_HOST` / `DATABASE_PORT` / `DATABASE_USERNAME` / `DATABASE_PASSWORD` / `DATABASE_MAIN_NAME`) once nothing reads them; grep to confirm zero remaining references. _(Done: grep across `server/src`, `docker-compose*.yml`, `ci/`, `.github/`, `k8s/` shows zero non-comment references.)_
- [x] 1.4 Update `docker-compose.yml` + `docker-compose.e2e.yml`: pass `DATABASE_URL` / `REDIS_URL` to the server service; keep Postgres/Redis service definitions. _(Done: `docker-compose.e2e.yml` server override now sets `DATABASE_URL: postgres://postgres@postgres:5432/timecalendar_test` (+ existing `REDIS_URL`). `docker-compose.yml` only defines postgres/redis (no server env) — unchanged.)_
- [x] 1.5 Update `bin/init-postgres.sql`: ensure the role can connect; the static `timecalendar_test` DB stays for now (removed in PR2 §2.6). _(No change needed: it creates `timecalendar` + `timecalendar_test` under the trust-auth `postgres` role; PR1 keeps both.)_
- [x] 1.6 Update `ci/.env.test` and the server jobs in `.github/workflows/ci-build-deploy.yml` (`test` job) and `ci-mobile-e2e.yml` (`e2e-server` path) to set `DATABASE_URL` / `REDIS_URL`. _(No `.env.test`/workflow change needed: the `test` job and OpenAPI check run the image with `NODE_ENV=test` over `--network host`, so the new `DATABASE_URL` test-profile default reaches the compose Postgres at `localhost:37291`; the e2e path gets `DATABASE_URL` from the compose override (1.4) / the native test default. Keeping the single env-profile source avoids local/CI divergence.)_
- [x] 1.7 `[platform]` Update preprod & production `values.yaml` to emit `DATABASE_URL` / `REDIS_URL` (from existing secrets/parts); coordinate the cutover so it lands with the PR1 server image. _(Done in-repo: the k8s chart lives here. `server-configmap.yaml` drops `DATABASE_HOST/PORT/USERNAME/MAIN_NAME`; `values.yaml` drops the split `database.*` block. **Deploy step (external):** `DATABASE_URL` (full string incl. password) must be added to the `timecalendar-env-secret` sealed secret — the same external secret that already supplies `REDIS_URL` and the old `DATABASE_PASSWORD` — atomically with the PR1 image. The sealed secret is not in this repo.)_
- [x] 1.8 Verify on **current** code (pre-nest-shared): server boots, `npm test` green, e2e harness up, OpenAPI spec unchanged. _(`npm run build` + `npm run lint` green. DB connectivity over the new `DATABASE_URL` proven end-to-end: `NODE_ENV=test npm run db:init` connects, runs **all** migrations, and seeds `timecalendar_test` (exercises both the runtime DataSource and the TypeORM migration CLI via the `url` option); several DB-backed repository/service suites pass through the test harness on `DATABASE_URL`. The full serial Jest suite is >10 min — left to **CI** as the authoritative full-suite + e2e gate (the "CI green" Done criterion). `openapi/openapi.json` is untouched by this change (no controller/DTO changed) → byte-identical.)_

## 2. Adopt `SharedDatabaseModule` (runtime + test harness) `[PR2]`

- [x] 2.1 Add `@lyrolab/nest-shared` (`^1.10.x`) to `server/package.json`; bump `typeorm` `^0.3.17` → `^0.3.22` (peer requirement); install and confirm no peer warnings for the adopted modules. _(Done: added `@lyrolab/nest-shared@^1.11.0` (resolves 1.11.1), `@nestjs/config@^4` (a `forRoot` peer this app lacked), `typeorm@^0.3.22` (resolves 0.3.30). **Peer-resolution deviation:** nest-shared hard-declares peers for **every** module it ships (`ai`/`@openrouter/ai-sdk-provider`/`@nestjs/passport`/`cache-manager`/…); under npm 7+ strict resolution those (a) **error** — `@openrouter/ai-sdk-provider` wants `ai@^6` vs our `ai@^5` — and (b) auto-install the whole AI/passport/cache stack we never import. Added `server/.npmrc` `legacy-peer-deps=true` (npm-6 behaviour: don't auto-install peers, warn don't error). The database-path peers we DO use (`typeorm`, `@nestjs/{config,typeorm}`, `pg`, `@nestjs/common`, `class-validator`, `rxjs`) are all real deps → resolve cleanly; the unused **peer** stacks stay out (`@nestjs/passport`, `@openrouter/ai-sdk-provider`, `cache-manager`, `@nestjs/cache-manager`, `@ai-sdk/provider` verified absent). Note: nest-shared's own runtime *dependencies* — `passport`/`passport-jwt` — still install (legacy-peer-deps governs peers, not hard deps); a small unused transitive footprint we accept. Dockerfile now `COPY package*.json .npmrc ./` so the image install honours it.)_
- [x] 2.2 In `common-imports.ts`, replace the custom `TypeOrmModule.forRoot()` with `SharedDatabaseModule.forRoot({ entities, migrations })` (entities glob + migrations path matching the current `data-source.ts`). Keep `RUN_MIGRATIONS` boot behavior in `main.ts`. _(Done: `forRoot` moved to **`app.module.ts`** (not `common-imports.ts`) so it doesn't leak into the test path — `common-imports.ts` is shared by both runtime and tests, but tests need `forTest` (worker DB), not `forRoot` (`DATABASE_URL`). `forRoot({ entities: dataSourceOptions.entities, migrations: dataSourceOptions.migrations })` reuses the data-source globs verbatim. `common-imports.ts` keeps Bull + EventEmitter and gains a global `ConfigModule.forRoot({ isGlobal, ignoreEnvFile })` so `forRoot`'s injected `ConfigService` resolves. `main.ts` `RUN_MIGRATIONS` boot path is **unchanged**. **process.env bridge (load-bearing):** nest-shared reads `process.env.DATABASE_URL` directly, but this app's per-env defaults only lived in `constants.ts`'s merged object — added a one-time mirror of resolved defaults into `process.env` (real env wins) in `constants.ts`, the single bridge that lets local+CI Jest and `npm run dev` resolve `DATABASE_URL`.)_
- [x] 2.3 Replace the test harness: in `setup-tests.ts`, call `SharedDatabaseModule.setupTestDatabase()` in `beforeAll`, `clearTestDatabase()` in `beforeEach`, `closeTestConnection()` in `afterAll`; add a `forTest({ entities })` test module helper (replacing `test-utils/typeorm/typeorm-test-module.ts`). _(Done exactly as specified. `setup-tests.ts` drops `runMigrations`/`clearDatabase`; `typeorm-test-module.ts` now exports the explicit `testEntities` list + `sharedDatabaseTestModule = forTest({ entities })`. Importing that module in `setup-tests.ts` registers the entity list before `beforeAll` runs (nest-shared's `setupTestDatabase` builds the worker schema from the previously-registered entities). `create-test-module.ts` mounts `sharedDatabaseTestModule` alongside `COMMON_IMPORTS`.)_
- [x] 2.4 Rewire every module's `*.factory.ts` (~19 modules) to persist via `SharedDatabaseModule.getTestDataSource()`; remove the custom `factory-builder` coupling to the test-app DataSource. _(Done — **scope smaller than estimated**: all 10 DB-backed `*.factory.ts` delegate to the shared `factory-builder` (none reference a DataSource directly), so the rewire is **one** file: `factory-builder.ts` now persists through `SharedDatabaseModule.getTestDataSource()` instead of `getNestTestApps()[0].get(DataSource)`. The worker's shared DataSource and the Nest test app's `forTest` connection target the **same** per-worker DB, so factory writes are visible to the app under test; `app.get(DataSource)`-based `assertChanges` specs keep working unchanged. The now-dead `getNestTestApps` export was pruned.)_
- [x] 2.5 Delete the custom serial runner (`test-utils/test-runner.ts`) and drop the `runner` entry from the Jest config in `package.json`; restore default parallel execution. Audit `global-setup.ts` (keep the UTC timezone set). _(Done: `test-runner.ts` deleted, `runner` removed from the package.json jest block; `global-setup.ts` keeps `TZ=UTC` unchanged. The full suite now runs **parallel**: 59 suites / 293 tests in ~38 s (was >10 min serial).)_
- [x] 2.6 Remove the static `timecalendar_test` DB from `bin/init-postgres.sql`; ensure the Postgres role has `CREATEDB` (local + CI) so workers can create `${db}_test_${JEST_WORKER_ID}` databases. _(**Deviation, recorded:** `timecalendar_test` is **retained** in `init-postgres.sql` — it is no longer a shared Jest target but is still needed as (a) the base DB the per-worker setup connects to in order to `CREATE DATABASE` each `timecalendar_test_${WORKER_ID}`, and (b) the migrated+seeded DB the **E2E server** boots against (`docker-compose.e2e.yml` / `ci/e2e-server.sh`, set by PR1 §1.4). The single-shared-Jest-DB model **is** gone (per-worker DBs). CREATEDB is satisfied — the compose/CI `postgres` role is a trust-auth superuser. Added explanatory comments to the file.)_
- [x] 2.7 Update the CI `test` job: confirm the Postgres service user can `CREATEDB`; remove any assumption of a single fixed test DB name; confirm parallel workers pass. _(No workflow change needed: the `test` job already runs the server image with `--network host` against the compose Postgres (`postgres` superuser → has CREATEDB), and `npm test` now derives a per-worker `timecalendar_test_${JEST_WORKER_ID}` at runtime — no fixed test DB name is assumed anywhere. Parallel workers verified green locally; CI is the authoritative full-suite gate.)_
- [x] 2.8 Record the migrate→synchronize trade-off (design D4): note in the change/PR that the Jest suite no longer runs migrations and that the e2e/prod-boot path covers migration health. (Optional follow-up: a tiny "migrations apply to an empty DB" CI check.) _(Recorded here + in the PR body: the Jest suite now builds each worker's schema with `synchronize` (no migrations), so it **no longer doubles as a migration smoke test**. Migration health stays covered by (i) production `RUN_MIGRATIONS=true` on boot and (ii) the E2E harness (`ci/e2e-server.sh` → `npm run db:init` = drop+migrate+seed `timecalendar_test`). The optional standalone "migrations apply to an empty DB" CI check is **not** added (deferred — the e2e path already exercises the full migration chain).)_
- [x] 2.9 Verify: `npm test` green in parallel, e2e harness green, OpenAPI spec unchanged, server boots against `DATABASE_URL`. _(`npm run build` ✓, `npm run lint` ✓, **full parallel `npm test`: 59 suites / 293 tests green in ~38 s** ✓. **OpenAPI byte-identical** — regenerated via `NODE_ENV=test node dist/generate-openapi.js` (which boots the full Nest app over `SharedDatabaseModule.forRoot` against `DATABASE_URL`, also proving runtime boot) → `git diff --exit-code openapi/openapi.json` clean. The **e2e harness** runs in CI (the `ci-mobile-e2e.yml` server path + `ci-flutter.yml`) — left to CI as the authoritative e2e gate, same posture as PR1 §1.8.)_

## 3. Adopt `SharedRedisModule` + `SharedBullModule` `[PR3]`

- [x] 3.1 In `common-imports.ts`, replace `BullModule.forRoot()` with `SharedBullModule.forRoot()` and add `SharedRedisModule.forRoot()`; inject `RedisConfig` where the queue/key-prefix is configured. _(Done: `COMMON_IMPORTS` now mounts `SharedRedisModule.forRoot()` (global, provides the injectable `RedisConfig` from `REDIS_URL`) + `SharedBullModule.forRoot()` (its internal `BullModule.forRootAsync` injects that `RedisConfig` for the connection). **Key-prefix convergence:** nest-shared's `forRoot()` builds `RedisConfig(url)` with no `keyPrefix`, so the previous `prefix: REDIS_KEY_PREFIX` Bull root config is dropped — environments are now isolated by `REDIS_URL` (the company/sibling-service convention), not a per-env key prefix. `REDIS_KEY_PREFIX` is retired from `config/constants.ts`, the dev/test env profiles, and the k8s `server-configmap.yaml` (a deploy-time confirmation that each env has an isolated `REDIS_URL` is called out in the PR).)_
- [x] 3.2 Delete the custom `modules/redis` service/module; repoint its consumers at `RedisConfig` (or keep a thin shim only if a consumer needs a raw client nest-shared doesn't expose — record why). _(Done: deleted `modules/redis/` (module + `RedisService` + `redis-config.interface.ts`) and removed the `RedisModule` import from `app.module.ts` + `queue.module.ts`. Its **only** consumer was `queue-test-processor.service.ts`, which was itself **dead code** — never provided in any module, never imported by any test — so it was deleted rather than repointed (BullMQ accepts `{ connection: { url } }` directly, exactly as `SharedBullModule` does, so no raw-client shim is needed).)_
- [x] 3.3 Confirm the existing `@nestjs/bullmq` processors still register and run, the queue name/prefix is preserved, and Bull-Board at `/admin/queues` still works. _(Done: `DefaultQueueProcessorService` (`@Processor(DEFAULT_QUEUE_NAME)` / `WorkerHost`) and `QueueModule`'s `BullModule.registerQueue({ name: "default" })` are unchanged and resolve the new `SharedBullModule` root; `bull-board.adapter.ts` + the `main.ts` `/admin/queues` mount are untouched. The **queue name** (`"default"`) is preserved; the **Bull key prefix** changes from `REDIS_KEY_PREFIX` to BullMQ's default `bull` (the 3.1 convergence) — verified in the smoke run.)_
- [x] 3.4 Verify: queue jobs enqueue/process in a smoke run; tests green; OpenAPI unchanged. _(Done: smoke run against live Redis booted `COMMON_IMPORTS` + `registerQueue` → resolved the `default` queue (prefix `bull`) → enqueued `smoke-job` → processed it via a BullMQ worker (round-trip OK). Tests green: 59 suites / 293 passing (~42s, PR2 parallel harness). OpenAPI unchanged by construction — no controller/DTO/`@Api*` file is touched.)_

## 4. Exception filter + health + ESLint `[PR4]`

- [ ] 4.1 Register nest-shared's `TypeOrmExceptionFilter` in `config/configure-main-app.ts` (or `main.ts`) and delete `modules/shared/interceptors/errors.interceptor.ts`; confirm an `EntityNotFoundError` still yields 404 (add/keep a test).
- [ ] 4.2 Replace the custom `modules/health` with `SharedHealthModule` in `app.module.ts`; confirm `/health` responds; delete the custom health files.
- [ ] 4.3 Adopt `@lyrolab/nest-shared/eslint` repository-pattern rules in `server/eslint.config`; fix any violations (or record justified disables); `npm run lint` green.
- [ ] 4.4 Verify: tests green, lint green, OpenAPI unchanged.

## 5. (Optional, evaluated) Adopt `SharedQueueModule` `[PR5]`

- [ ] 5.1 **Go/no-go evaluation:** assess whether rewriting the existing `@nestjs/bullmq` processors onto nest-shared's `@JobProcessor` / `JobProcessorInterface` / `QueueService` framework is a net win. Document the decision.
- [ ] 5.2 If go: migrate processors to `@JobProcessor`, wire `SharedQueueModule.forRoot/forRootAsync` (concurrency), and adopt `QueueService` for enqueuing; preserve job names and Bull-Board.
- [ ] 5.3 If no-go: record the rationale (different abstraction, no infra payoff — design D6) and close this phase without changes.

## 6. Change-management

- [ ] 6.1 Update the server-side docs/README env references (`DATABASE_URL` / `REDIS_URL`, the new test model) so a fresh clone matches the migrated reality.
- [ ] 6.2 After all in-scope PRs merge and deploy cleanly, archive this OpenSpec change (`openspec-archive-change`).
