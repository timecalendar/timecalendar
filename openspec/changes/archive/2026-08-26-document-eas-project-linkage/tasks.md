## 1. Reconcile the distribution contract

- [x] 1.1 Update the main `mobile-distribution` requirement from the obsolete
      pre-initialization placeholder behavior to the real initialized EAS project linkage.
- [x] 1.2 Verify the updated requirement matches `mobile/app.config.ts` and the binding EAS
      Architecture Book entry without adding credentials or changing release behavior.

## 2. Repair documentation gates

- [x] 2.1 Format every Markdown file touched by the branch with the repository Prettier
      configuration.
- [x] 2.2 Validate all OpenSpec changes/specs and check Prettier against the complete branch
      diff from `origin/main`.
