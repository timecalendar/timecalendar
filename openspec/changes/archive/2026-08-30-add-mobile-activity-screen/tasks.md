> Every path below is relative to the repository root. All commands run from `mobile/`.
> Read first: `docs/react-native-migration/05-tech-specs/activity-revival.md` (**Product behavior** in full, architecture decisions 6/8/9, **Verification strategy → React Native UI tests**), this change's `design.md` **D1–D12**, and the Architecture Book's `navigation.md`, `theming.md`, `accessibility.md`, `i18n.md`, `features.md`, `testing.md`, `definition-of-done.md`.
> `mobile/src/features/activity/data/` **already exists** ([TIM-396](/TIM/issues/TIM-396) `b378adb8`, [TIM-397](/TIM/issues/TIM-397) `ed4fbf22`). Read `repository.ts`, `coordinator.ts`, `types.ts` and `index.ts` first — the repository API and the two outcome unions are **fixed inputs**, not things to design. This change adds reads and a `ui/` layer over them and changes **no** coordinator policy.
> **Reference implementations to copy rather than reinvent:** `features/calendar/ui/agenda-list.tsx` (SectionList + RefreshControl), `features/calendar/ui/calendar-screen/calendar-screen-status.tsx` (compact retryable error), `features/hidden-events/ui/hidden-events-screen.tsx` (feature screen + `Stack.Screen` title + empty state), `features/calendar-sources/data/user-calendars/hooks.ts` (reactive `useLiveQuery` read + its test).

## 1. Theme tokens (`src/theme/tokens.ts`)

- [x] 1.1 Add `positive` and `informational` to **both** `Colors.light` and `Colors.dark` (D7). Proposed values, re-verified before committing: `positive` `#146C43` / `#7EE2A8`; `informational` `#0B57D0` / `#A8C7FA`. A light-only token is incomplete (`theming.md`).
- [x] 1.2 Add a **STATUS** block to the contrast documentation in the same file, in the existing style, recording each token's computed ratio on `background` and on `backgroundElement` for its scheme. Computed with the WCAG 2.x relative-luminance formula: positive light 6.45 / 5.67, positive dark 13.33 / 10.10, informational light 6.39 / 5.61, informational dark 12.21 / 9.25. Recompute; if your numbers differ from these, use yours and say so in the change notes.
- [x] 1.3 Do **not** amend the existing `destructive` rows, even if a fresh computation disagrees with the committed number — note the discrepancy in the change notes and leave the block alone (D7).
- [x] 1.4 Do **not** add `positiveSoft` / `informationalSoft` surface variants. R-2: nothing in this screen fills a surface with a status color.
- [x] 1.5 Extend `src/theme/theme.test.tsx` so `useTheme` resolves both new tokens to their expected light **and** dark values, matching how the file already proves `primary`.

## 2. Activity reactive reads and the badge rule (`src/features/activity/data/`)

- [x] 2.1 Add `data/hooks.ts` with `useActivityLogs(): { logs: ActivityLog[]; loaded: boolean }` — one `useLiveQuery` over `activityLogs` ordered `desc(createdAt), desc(id)` (the same tie-break `listActivityLogs` uses), rows mapped through the existing `rowToActivityLog` with nulls dropped, memoized like `useUserCalendars`. `loaded` is `updatedAt !== undefined`. Import `useLiveQuery` from `@/db` (the seam re-export), never from `drizzle-orm/expo-sqlite`.
- [x] 2.2 One hook returning both, deliberately unlike the `useUserCalendars` / `useUserCalendarsLoaded` pair: two calls would open two live subscriptions over the same table. Comment the divergence so it does not read as an oversight.
- [x] 2.3 Add `useActivityState(): ActivityState` to the same file — one `useLiveQuery` over `activityState`, mapped through the same defaults a missing singleton row produces for `readActivityState` (reuse that mapping; do not restate the defaults).
- [x] 2.4 Add `data/unread-badge.ts` with `formatUnreadBadge(count: number): string | null` (D8): `null` below 1, `String(count)` for 1–99, `"99+"` from 100. Pure, no React, no `t()`. Comment why it lives in `data/` and not `ui/`: Settings must consume it without importing an Activity screen module, and `data/` is under the 90% logic gate.
- [x] 2.5 Extend `data/index.ts` and the feature barrel `src/features/activity/index.ts` with `useActivityLogs`, `useActivityState` and `formatUnreadBadge`, keeping each file's existing ordering convention. B-2: the sublayer must not import its own feature barrel.
- [x] 2.6 Do **not** change `coordinator.ts`, `request.ts`, `repository.ts` or `mappers.ts`. If you believe you must, stop and say why on the ticket — Ticket 6 is building on those files in parallel.

## 3. Activity presentation logic — pure modules (`src/features/activity/ui/`)

Keep these free of JSX so they are unit-testable without rendering.

- [x] 3.1 Add `ui/activity-items.ts`: `buildActivitySections(logs: ActivityLog[])` → one section per log, ordered newest first, `data` = the log's changes flattened as discriminated items in the order **new → changed → cancelled** (D3). Map the contract exactly: `calendarChange.newItems` → new, `calendarChange.changedItems` → changed, `calendarChange.oldItems` → **cancelled** (server-confirmed: `find-removed-and-changed-events.ts` pushes an old event to `oldItems` only when it has no corresponding new event).
- [x] 3.2 A log whose three arrays are all empty produces **no section** (D3) — a header announcing a change with nothing under it is worse than silence.
- [x] 3.3 Give each item a stable key combining the log id, the kind and the event uid, so two logs touching the same event do not collide.
- [x] 3.4 Add `ui/describe-change.ts`: `describeChangedItem(previous, next)` → the ordered list of _meaningful_ differences only (D7 of the specification's product behavior) — time (either endpoint differs), location (differs, including null↔value), title (differs). Return a discriminated field plus its `from` / `to` display strings; return an empty list when nothing comparable differs, and let the item still render its title.
- [x] 3.5 Timestamps arrive as ISO strings. Parse defensively: an unparseable `startsAt` / `endsAt` renders the item **without** its time line rather than throwing or printing `Invalid Date`. Mirror the repository's posture — a bad row degrades, it does not crash the timeline.
- [x] 3.6 Formatting comes from `@/features/calendar/data` (D6) — `resolveLocale`, `formatFullDateTime` (group header), `formatEventDateRange(start, end, locale, allDay, zone)` with `allDay: false` (an item's time; note the argument order). Zone from `useDisplayZone()` (`@/features/settings/prefs`), resolved in the screen and passed down. Do **not** re-derive a `date-fns` locale map inside Activity.

## 4. The Activity screen (`src/features/activity/ui/activity-screen.tsx` + `ui/index.ts`)

- [x] 4.1 `<SectionList>` over `buildActivitySections` (D3): `stickySectionHeadersEnabled={false}`, `refreshControl`, `onEndReached`, `ListFooterComponent`. No FlashList, no new dependency (the agenda's precedent).
- [x] 4.2 Section header: a `View` with `accessibilityRole="header"` carrying the calendar name (`ThemedText type="subtitle"`) and the change time (`type="small"`, `themeColor="textSecondary"`), plus one combined translated `accessibilityLabel` — mirroring `AgendaList`'s `DayHeader`. Both values wrap; neither uses `numberOfLines` in a way that clips.
- [x] 4.3 Item rendering: each item shows its **translated kind label** in the kind's token colour (`positive` / `informational` / `destructive`) plus the event title and time; a changed item additionally lists only `describeChangedItem`'s differences (D7). Colour is never the only signal — the label is always present.
- [x] 4.4 New and changed items are `Pressable` with `accessibilityRole="button"` and a translated label naming the change and saying it opens details; on press, `router.push(\`/event-details/${uid}\`)`— the **literal URL string**, using the new item's uid and, for a changed item, the **new version's** uid (D5). Do **not** import`eventRoute` or anything else from the calendar feature for navigation.
- [x] 4.5 A cancelled item is a plain `View` (D12): no `onPress`, no `accessibilityRole="button"`, no hit target. Give it a `testID` so the test can prove pressing it navigates nowhere.
- [x] 4.6 Pull-to-refresh: `RefreshControl` wired to `refreshNewestPage({ force: true })` with a local `refreshing` flag and a translated `accessibilityLabel`, mirroring `calendar-screen.tsx`. Map outcomes to the refresh-error flag exactly per **D10** — `no-calendars` clears it (D11), `too-many-calendars` sets it.
- [x] 4.7 Older pages: `onEndReached` → `loadOlderPage()` when `!olderPageComplete`, nothing is already in flight, and at least one row is rendered. Map outcomes per **D10**: `cursor-reset` is **not** an error. Do not blank or replace rendered rows while a page is in flight (the rows come from the cache; only the footer changes).
- [x] 4.8 Footer: a spinner while an older page is loading, an inline message + 44pt retry when one failed, nothing when the chain is complete.
- [x] 4.9 States per **D10**'s second table: loading (until `loaded`), empty, populated, cached failure (rows kept + compact retryable banner above them, the `calendar-screen-status` shape), empty failure (full retryable state, and the empty sentence is **not** shown). Error text is `accessibilityLiveRegion="polite"` with an `alert` role.
- [x] 4.10 Read-state effect (D2): on mount, and again whenever the reactive `unreadCount` becomes non-zero, call `markActivityReadFromCache()`. Comment at that call site that `markActivityRead(asOf)` is deliberately **not** called here and is not dead code — it is a specified repository operation whose first caller is Ticket 6 (design D2). Never write a device-clock value as the watermark.
- [x] 4.11 **Add no mount-time refresh** (D1). The five-minute screen-open and app-foreground policy is [TIM-399](/TIM/issues/TIM-399)'s. Keep the screen's controller logic in one place so Ticket 6 inserts one hook call.
- [x] 4.12 `<Stack.Screen options={{ title: t("activity.title") }} />` at the top of the screen, as `HiddenEventsScreen` does. Theme from `@/theme` only — no literal colours, no `allowFontScaling={false}`, minimum 44pt iOS / 48dp Android on every touchable.
- [x] 4.13 Add `ui/index.ts` exporting `ActivityScreen` (and nothing the route does not need).

## 5. Route registration

- [x] 5.1 Add `src/app/activity.tsx` containing exactly `export { ActivityScreen as default } from "@/features/activity/ui"` — one line, no test colocated in `src/app/` (`navigation.md`: Metro bundles every `*.tsx` there).
- [x] 5.2 Register it in `src/app/_layout.tsx` as a `Stack` sibling of `(tabs)` with `options={{ headerShown: true }}`, beside `hidden-events` / `notification-settings`, with the same comment style naming the deep link `timecalendar-dev://activity`.
- [x] 5.3 Extend `src/components/settings-route-structure.test.ts` with an Activity case proving both halves: the route file is the thin re-export, and the root layout registers `name="activity"` with `headerShown: true`.

## 6. The Settings Activity row (`src/features/settings/ui/`)

- [x] 6.1 `settings-row.tsx`: add an optional `badge?: string` to the router/action row props, rendered between the text block and the chevron, `accessible={false}` + `importantForAccessibility="no-hide-descendants"` like the existing icon and chevron (D9). Pill shape from `Radii.pill`, `primaryStrong` fill with `onPrimary` text (the documented AA body pair — never the bright identity pink under white text). It must not change the row's height below the platform minimum, and it must shrink before the label does at large font scales.
- [x] 6.2 `settings-screen.tsx`: add the Activity destination **first** in the `events` section — `href: "/activity"`, `testID: "settings-activity"`, icon `{ ios: "clock.arrow.circlepath", android: "history", web: "history" }`, label/hint keys from §7 — with an `unreadBadge: true` marker on the entry so the section stays declarative and only the badge value is resolved reactively.
- [x] 6.3 Read the count with `useActivityState().unreadCount` from `@/features/activity` (the feature barrel) and pass `badge={formatUnreadBadge(count) ?? undefined}` for the marked entry only. Settings must not read `activity_logs` and must not import `@/db` (B-3).
- [x] 6.4 Compose the row's single accessible name from the label and a **pluralized, un-capped** unread count — the badge shows `99+`, the announcement says the real number. Reuse the `{{primary}}, {{secondary}}` label shape the calendar-summary row already uses.
- [x] 6.5 The row is unconditional: not gated on notification preferences, not gated on held-calendar count, no feature flag.

## 7. Copy (`src/i18n/locales/en.json`, `src/i18n/locales/fr.json`)

Flat dotted keys; EN is the canonical key set and `tsc` enforces bidirectional parity (`i18n.md`).

- [x] 7.1 The empty-state sentences are **frozen verbatim** by the ticket. EN: `No recent changes. Timetable updates will appear here.` FR: `Aucune modification récente. Les changements d'emploi du temps apparaîtront ici.` Note in the change notes that this FR string keeps the **ASCII** apostrophe the ticket froze, diverging from the catalog's typographic `’` convention (`aujourd’hui`); it is one character and the ticket's wording is the acceptance criterion.
- [x] 7.2 Add the `activity.*` keys the screen needs, in both catalogs: screen title; empty; cached-failure message; empty-failure message; retry label + its accessible name; older-page failure message, retry and its accessible name; pull-to-refresh accessible name; group-header accessible name; the three kind labels; the per-item accessible names for new and changed (naming the event and saying it opens details); the changed-field labels (time, location, title) and the `from → to` line plus its spoken form ("… changed from … to …" — a screen reader should not have to read an arrow).
- [x] 7.3 Add `settingsHub.activity.label`, `settingsHub.activity.hint`, `settingsHub.activity.accessibilityLabel`, and the pluralized `settingsHub.activity.unread_one` / `_other`.
- [x] 7.4 No user-facing literal escapes the catalogs (`i18next/no-literal-string` covers `accessibilityLabel` / `accessibilityHint` too). `"99+"` is not a catalog key — it is a numeral and a plus sign (D8).

## 8. Tests

Two layers (`testing.md`): the `data/` suite proves the reads against the mocked `@/db` seam; the `ui/` screen suite mocks the feature's own `data/` hooks and keeps the pure helpers real. Await every RNTL 14 helper; reset suite-owned mock state in `afterEach`; never `jest.restoreAllMocks()`.

- [x] 8.1 `data/hooks.test.ts` — follow `calendar-sources/data/user-calendars/hooks.test.ts` exactly (mock `@/db`, drive `useLiveQuery`): rows map to domain newest-first, an undecodable row is dropped rather than fatal, `loaded` is false before the query settles and true after, and a missing state row reads as the documented defaults.
- [x] 8.2 `data/unread-badge.test.ts` — 0 → no badge; 1, 42, 99 → their decimal; 100 and 5,000 → `"99+"`. Include a negative count as a defensive case.
- [x] 8.3 `ui/activity-items.test.ts` — groups newest first; children ordered new → changed → cancelled; `oldItems` map to **cancelled** and `newItems` to **new** (the inversion this test exists to catch); an all-empty change payload produces no section; keys are unique when two logs touch the same event.
- [x] 8.4 `ui/describe-change.test.ts` — a location-only change lists only the location; a time-only change lists only the time; a title change is listed; identical previous/new yields an empty list; `null ↔ value` location counts as a difference; an unparseable timestamp degrades to no time line.
- [x] 8.5 `ui/activity-screen.test.tsx` — **every state in both locales** (loading, empty, populated, cached failure, empty failure), with the two empty-state sentences asserted **verbatim**. Switch locale the way the repo's existing localized screen tests do.
- [x] 8.6 Same file — pull-to-refresh fires the RefreshControl's `onRefresh` and calls `refreshNewestPage` with `{ force: true }` exactly once (`calendar-screen.test.tsx:352` shows how to reach the control); the footer retry calls `loadOlderPage` and **not** `refreshNewestPage`; `onEndReached` calls `loadOlderPage`; a completed chain calls it no further.
- [x] 8.7 Same file — a new item navigates to `/event-details/<new uid>`; a changed item navigates with the **new version's** uid (make the previous and new uids differ, or the test proves nothing); pressing a cancelled item navigates nowhere and the node exposes no button role.
- [x] 8.8 Same file — outcome mapping (D10): a `failed` refresh with cached rows keeps every row and shows the compact message; a `failed` refresh with no rows shows the full retry state and **not** the empty sentence; `no-calendars` leaves the empty state with no error; `cursor-reset` shows no error.
- [x] 8.9 Same file — read state (D2): mounting calls `markActivityReadFromCache`; a stored unread count becoming non-zero while mounted calls it again; nothing calls `markActivityRead`.
- [x] 8.10 Same file — long content: a calendar name, an event title and a location each far longer than a line, plus a group carrying **hundreds** of changed items, render without throwing and without a clipping `numberOfLines` on the wrapping values. This is the standing proof for D3's flattening; keep it.
- [x] 8.11 Accessibility: group headers resolve to `getByRole("header")` through the real accessibility tree (the `themed-text.test.tsx` posture — assert the resolved semantic, not that a prop was passed); error text carries the live-region + `alert` pair.
- [x] 8.12 `settings-screen.test.tsx` — Activity renders **in the Events section** and its full accessible row routes to `/activity`; the badge renders `1`, `99`, `99+`, and is absent at zero; the row's accessible name carries the un-capped count; the row renders with zero calendars.
- [x] 8.13 `settings-row.test.tsx` (or the screen suite if the row has no own file) — the badge is not separately focusable and the row remains one touch target.
- [x] 8.14 Do **not** edit any pre-existing test other than `settings-screen.test.tsx`, `settings-route-structure.test.ts` and `theme.test.tsx`. Any other required edit means this change regressed a consumer — report it instead of absorbing it.

## 9. Gates

- [x] 9.1 `npx tsc --noEmit` — green (this is also the FR/EN parity gate).
- [x] 9.2 `npm run lint` — green, including `i18next/no-literal-string`, the `react-native-a11y` touchable rules, and the `timecalendar/activity-seam` boundary (the `ui/` layer must reach neither `@/db`'s Activity tables nor the generated calendar-log client).
- [x] 9.3 `npm test -- --coverage` — green. Run the **coverage** form; plain `npm test` passes blind past the threshold. `src/features/activity/!(ui)/**` stays at **90% lines and branches**; the project stays above the 70% global floor. Do not reach either number with `istanbul ignore`.
- [x] 9.4 `npm run format` (or the repo's Prettier check) clean.
- [x] 9.5 Re-confirm the diff touches **no** sensitive surface: no `openapi/openapi.json`, no `mobile/src/api/generated/**`, no `mobile/app.config.ts`, no `eas.json`, no `mobile/firebase/**`, no `server/**`, no migration, no new runtime dependency. If one of these appears in `git status`, stop and raise it on the ticket — the slice is wrong.

## 10. Architecture Book

- [x] 10.1 `features.md`: extend the `activity` row with the `ui/` half — the `/activity` route, the grouped timeline, the Settings unread entry, the reactive reads, and the two operations the screen calls. Record that the screen-open and foreground policy is **not** here (D1).
- [x] 10.2 `theming.md`: record the two semantic status tokens beside the brand-token entry, and add them to the contrast section's list of documented pairs (D7).
- [x] 10.3 `navigation.md`: add `/activity` to the root-Stack sibling inventory in the same style as `/hidden-events` and `/notification-settings`, naming the deep link and the thin re-export.
- [x] 10.4 No new ADR. Judged against `decisions/README.md`'s policy: the load-bearing Activity decisions are already recorded (ADR [046](../../../docs/mobile/architecture-book/decisions/046-activity-cache-merge-and-server-read-watermark.md) for the merge + watermark, ADR [048](../../../docs/mobile/architecture-book/decisions/048-activity-refresh-single-flight-and-token-precondition.md) for single-flight + the token precondition), and this change's decisions — a palette addition, a list shape, a route registration — are cheap to reverse and belong in the topical files above. **If you conclude otherwise while implementing, pick the number immediately before writing:** re-read `decisions/` for the live maximum _and_ check open mobile PRs (`gh pr diff <N> --name-only | grep decisions/`); the index records a reserved gap and a collision is invisible to git.
- [x] 10.5 Append a dated entry to `docs/mobile/architecture-book/CHANGELOG.md` (that is the real filename — the `.claude/rules/mobile.md` pointer calls it `architecture-changelog.md`) naming the new tokens, the `/activity` route, the Settings entry, and the Ticket-5/Ticket-6 boundary (migration-approach §7).

## 11. Definition of Done, evidence, hand-back

- [x] 11.1 Walk `definition-of-done.md` and record a reason for anything that does not apply. Applicable here: gates + coverage, FR/EN parity, contrast, live-region announcements, touch targets, font scaling, Book updated. **Not** applicable: no Maestro flow (Ticket 7 owns the Activity E2E flows), no analytics, no migration.
- [x] 11.2 **Render evidence, recorded by you on [TIM-398](/TIM/issues/TIM-398)** — this is Applier acceptance work, never a hand-back. The device host has no emulator (`docs/agent-dev-environment.md`), so capture it from the screen suite: render each of populated, empty, cached-error and empty-error in **both** locales and post the rendered text trees (condensed) as a ticket comment, one block per state per locale. Do not commit snapshot files for this.
- [x] 11.3 Write `docs/react-native-migration/inbox/<date>-activity-screen-device-pass.md` as a `(HUMAN: …)` note for the real-device pass that CI cannot do: VoiceOver/TalkBack focus order and announcement quality, Dynamic Type at the largest scale, the two status colours in both schemes on real panels, and smooth scrolling of a large group on a low-end Android. Follow the inbox README's shape. It does **not** block this PR.
- [x] 11.4 `openspec validate add-mobile-activity-screen --strict` passes. Run it **early**, not at merge time: only `openspec archive` validates delta headers and it aborts behind the long CI gate. Note the `mobile-settings-hub` delta is a **MODIFIED** requirement whose header must match the deployed one byte-for-byte.
- [x] 11.5 Comment on [TIM-399](/TIM/issues/TIM-399) with the two cross-ticket facts this change deliberately left open: (a) the screen has **no** mount-time refresh, so Ticket 6 owns the five-minute screen-open and foreground policy and inserts it as one hook call in `ActivityScreen` (D1); (b) `markActivityRead(asOf)` still has no caller, and Ticket 6 — which owns the triggers that produce an `asOf` while the screen is visible — is its natural first caller (D2). Neither is dead code and neither should be deleted.
- [x] 11.6 PR body: state that no sensitive surface is touched, that accessibility and FR/EN parity are binding Book rules met by §8/§9, and that the screen ships without its automatic triggers by design (D1) with the epic's Ticket 6 landing them in parallel.
