## 1. Lock civil-week and label semantics

- [x] 1.1 Extend `mobile/src/features/calendar/data/week.test.ts` first with Monday-first seven-date weeks, five-date weekend filtering, explicit alternate first-weekday input, stable chronological day keys, month/year crossings, and DST gap/repeat weeks in multiple display zones; run this exact suite.
- [x] 1.2 Add the smallest pure exported week-column model under `mobile/src/features/calendar/data/` using `startOfWeekInZone`, `addDaysInZone`, civil day keys, explicit first-weekday input, and Saturday/Sunday identity; make introduced statements/branches 100% covered and keep React, locale formatting, storage, and device globals out of the module.
- [x] 1.3 Extend `mobile/src/features/calendar/data/format.test.ts` for compact weekday/date parts at French/English narrow-width examples and display-zone day boundaries; retain every existing formatter result and run this exact suite.

## 2. Add the default-on persisted weekend preference

- [x] 2.1 Extend settings preference store tests first for missing → `true`, explicit true/false round trips, malformed/backend behavior as applicable, and restart-shaped reads; add the namespaced boolean key and total `getShowWeekends`/`setShowWeekends` API through `@/storage`, then run `prefs/store.test.ts`.
- [x] 2.2 Extend settings hook tests first for default value, reactive false/true writes, and remount persistence; add a stable `useShowWeekendsPreference` API backed by `useStoredBoolean` and export it through the settings barrels, then run `prefs/hooks.test.ts`.
- [x] 2.3 Add the weekend key to `mobile/src/storage/index.ts` and classify it as environment-independent; extend `mobile/src/storage/storage.test.ts` and affected environment-reset suites to prove centralized type coverage and preservation while backend-bound values clear, then run every edited storage/reset suite.

## 3. Expose Settings > Calendar without adding a route

- [x] 3.1 Add typed English and French keys for the Show weekends label and any required accessibility text; preserve bidirectional catalog parity and run the i18n suite plus TypeScript after the catalog edit.
- [x] 3.2 Extend `mobile/src/features/settings/ui/settings-screen.test.tsx` first to require one switch in the existing top Calendar section, default/false state, setter wiring, chronological source order, localized label, valid switch semantics, and the retained calendar-management link on iOS and Android.
- [x] 3.3 Render a controlled native `Switch` row beside calendar management in the existing Calendar section, using the settings preference hook, theme/spacing conventions, and platform minimum target; keep the generic Preferences destinations and routes unchanged and run the Settings screen suite.

## 4. Render aligned five/seven-day week columns

- [x] 4.1 Extend `mobile/src/features/calendar/renderer/owned-calendar-shell.test.tsx` first for one pinned committed header below the native title, a gutter-width spacer, localized Monday-to-Sunday labels, five/seven equal-flex cells, matching vertical column boundaries on all three pages, deterministic Today identity/non-color cue, and hidden decorative/neighbour semantics.
- [x] 4.2 Pass explicit first-weekday, weekend visibility, and a controllable current instant/day identity into the smallest renderer seam that keeps production call sites simple; derive the committed header and every page grid from the pure week-column model rather than device-local fields or index slicing.
- [x] 4.3 Render the date row outside the vertical `ScrollView`, retain one native vertical owner and one native horizontal `PagerView`, and add equal-flex clock columns/vertical boundaries without changing T03 horizontal-line/hour geometry, raw vertical-offset settlement, three direct pager children, or production diagnostics.
- [x] 4.4 Add a shape/typography Today distinction in addition to any theme color and localized Today semantics without making headers pressable or creating a second page heading; verify no current-time line, automatic scroll, selectable date, event tile, day mode, zoom, fallback, or extra pager appears.
- [x] 4.5 Run the renderer suite after each structural step and cover seven-to-five-to-seven rerenders, held/unsettled and accepted page transitions, generation replacement, vertical scrolling/background restoration, reduced motion, narrow widths, and unchanged cancellation/stale-callback behavior.

## 5. Wire Calendar while preserving seven-day context

- [x] 5.1 Extend `mobile/src/features/calendar/ui/calendar-screen.test.tsx` and controller tests first for explicit Monday-first policy, reactive weekend input, five/seven renderer props, unchanged seven-day Agenda range including fabricated weekend events, seven-civil-day page settlement, Today identity, and retained vertical offset across Week/Agenda and preference changes.
- [x] 5.2 Read the weekend preference at the Calendar screen/settings boundary and pass it into the owned renderer without adding MMKV access to Calendar data/renderer modules or turning a preference change into a transition revision/announcement.
- [x] 5.3 Re-run focused Calendar controller/screen regressions for mount/remount, Today, one-shot focus date, page revision/settlement/cancellation, Week/Agenda switching, sync status, checklist progress, fabricated synced/personal weekend event activation, details routing/return, Add, device clock preference, and settled announcements.

## 6. Update executable contracts and the Architecture Book

- [x] 6.1 Update `mobile/calendar-owned-shell.contract.test.ts` as the CI proof test for the intended T04 helper/settings/renderer inventory while continuing to reject calendar-kit, fallback/compatibility/duplicate renderers, a second pager or motion owner, unbounded pages, direct MMKV imports outside the seam, unexpected dependencies/sensitive surfaces, or loss of the three established Maestro journeys/Agenda helper; run the contract suite.
- [x] 6.2 Update `docs/mobile/architecture-book/calendar.md`, `storage.md`, `features.md`, and `CHANGELOG.md` with the current T04 dated-column, explicit week policy, Today cue, default-on Settings preference, five/seven layout, seven-day paging, unchanged Agenda, and still-absent later-capability contracts. Add no ADR unless implementation evidence changes an approved costly-to-reverse decision.
- [x] 6.3 Re-read the implemented delta against D01, D02, D04, D06, the canonical T04 ticket, delivery protocol, `mobile/AGENTS.md`, and exact Expo SDK 56/React Native accessibility documentation; stop and re-brief before changing an approved boundary or adding a dependency, route, alternate renderer, custom gesture owner, or sensitive-surface edit.
- [x] 6.4 Run `openspec validate add-owned-calendar-weekday-columns --strict`; repair proposal, design, spec, and tasks together if implementation evidence changes the feature shape, and do not weaken a requirement to fit an implementation.
- [x] 6.5 Create or update a migration inbox note tagged `(HUMAN: owner device verification)` with immutable build/revision, fabricated week beginning 2026-09-14, Settings path, restart/reset/launch instructions, exact device/OS/physical-or-simulator/build fields, French/English and month-boundary cases, prior accepted regressions, and the unchecked T04 owner checklist.

## 7. Produce local-green, CI, and owner evidence

- [x] 7.1 From `mobile/`, run every edited suite plus focused week/day-key, format, settings store/hooks/screen, storage/reset, renderer, controller/Calendar screen, i18n, repository-contract, Maestro selector, and harness tests with exact `npm test -- --runTestsByPath ...` paths; record commands, suite/test counts, outcomes, and tested Git revision.
- [x] 7.2 Run focused coverage proving 100% statement/branch coverage for introduced pure civil-week logic while preserving project thresholds; also run `npx tsc --noEmit`, `npm run lint`, scoped `npx prettier --check <edited-files>`, `npm run react-doctor:changed`, `git diff --check`, the established Maestro selector/harness contracts, and strict OpenSpec validation.
- [x] 7.3 Record content-free evidence from each available iOS/Android runtime with exact build/device/OS: seven dated columns, five-column redistribution, header/grid alignment while vertically scrolling and horizontally paging, non-color Today cue, French/English narrow labels, month crossing, switch false/true, restart persistence, seven-day page steps, unchanged weekend Agenda content, reduced motion, and retained earlier interactions.
- [x] 7.4 Mark unavailable native readability, Dynamic Type, restart, alignment, gesture, contrast, VoiceOver/TalkBack, or physical-device observations pending for the ticket-specific owner QA instead of converting Jest or host diagnostics into native claims; any explicitly accepted intermediate deferral must name T28 and preserve final product gates.
- [x] 7.5 Run a final diff audit for explicit first-weekday input, weekday-identity filtering, one shared five/seven model per render, header/grid alignment, no device-local date arithmetic, seven-day Agenda/page semantics, one horizontal/vertical motion owner, settled offset preservation, duplicate semantics, later-slice scope, and changes under OpenAPI/generated API, server migrations, native/store config, deployment/CI, or legacy Flutter paths.
- [x] 7.6 Push the tested implementation revision to this same draft PR and update its feature-level body with exact local/native evidence, build/fixture instructions, sensitive-surface statement, T04 limitations, and remaining owner checks; run the disclosure scan on the exact title/body before publication and re-read the stored PR body after every write.
- [x] 7.7 Confirm the PR head equals the exact revision used by the reported standard CI run and every required check is green; treat the focused pure coverage and `mobile/calendar-owned-shell.contract.test.ts` as direct CI proof for the introduced contract.
- [x] 7.8 Supply the testable build/revision and canonical T04 checklist for explicit owner QA, keep failures and focused retest evidence on this same ticket, and do not start the next renderer slice before acceptance and merge are recorded.

## 8. Rework the dated header onto native pager progress

- [x] 8.1 Replace the static committed date row with one vertically pinned, clipped three-slot header strip derived from the existing previous/current/next page records; keep the fixed header spacer aligned with the hour gutter and add no second `PagerView`, horizontal responder, or gesture handler.
- [x] 8.2 Bind the passive header-strip transform to the installed `PagerView`'s native page-scroll `position` and `offset`, using the measured pager lane width and existing animation runtime so drag, fling, programmatic paging, and snap-back share one progress source; keep transition requests and commits on the existing selected-page/idle revision path.
- [x] 8.3 Preserve exactly one accessible committed date row by hiding moving neighbour slots, and keep the native title, adjustable canvas label, Agenda range, anchor, and announcements on the settled revision until accepted idle settlement; reduced-motion actions must use the same pager path without a second animation owner.
- [x] 8.4 Extend `mobile/src/features/calendar/renderer/owned-calendar-shell.test.tsx` first to prove forward/backward fractional progress maps header and grid in the same direction, five/seven slots share column identity, vertical movement leaves the header pinned, and accepted settlement recenters the destination generation exactly once.
- [x] 8.5 Add cancellation regressions for native snap-back, AppState inactivity, generation replacement, preference/geometry replacement, and stale old-generation page-scroll callbacks; prove both pager and header return to center without a transition commit or announcement.
- [x] 8.6 Update `mobile/calendar-owned-shell.contract.test.ts` and `docs/mobile/architecture-book/calendar.md` so CI/current-state guidance require one native pager owner plus its passive synchronized header projection and reject a static dated header, second pager, independent timer/animation, or duplicate accessibility context.
- [x] 8.7 Run the edited renderer/Calendar/contract suites, focused coverage, TypeScript, lint, scoped Prettier, React Doctor changed-code review, `git diff --check`, and strict OpenSpec validation; then replace stale PR evidence with results bound to the new head and leave native drag/fling/snap-back alignment for explicit owner-device QA.
