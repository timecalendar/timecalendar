// The tabs group's layout is the native tab bar. Routes outside this group
// (e.g. src/app/schools.tsx) are reachable via the root Stack, not as tabs.
export { default } from "@/components/app-tabs"
