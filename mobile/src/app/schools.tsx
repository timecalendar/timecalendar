// Thin route entrypoint. Expo Router bundles every file under src/app/ as a
// route, so the screen's logic (and its colocated test) lives in a module.
export { default } from "@/components/schools-screen"
