// Thin route entrypoint (route-structure rule): the intranet-guidance step. Its
// Continue edge to /onboarding/import is the future assistant insertion point.
export { ConnectScreen as default } from "@/features/onboarding/ui"
