# About reachability flow (TIM-183). Shared across iOS and Android; the device
# locale is EN, so assertions use stable English catalog values.
appId: fr.samuelprak.timecalendar.dev
---
- launchApp:
    clearState: true
- stopApp
- openLink: timecalendar-dev://about
- runFlow:
    when:
      platform: iOS
    commands:
      # iOS 26 can re-present the first custom-scheme confirmation after the
      # initial tap changes SpringBoard's hierarchy. A second optional tap is
      # inert when the app already opened and dismisses that observed replay.
      - tapOn:
          text: "Open"
          optional: true
      - tapOn:
          text: "Open"
          optional: true
- extendedWaitUntil:
    visible: "Privacy policy"
    timeout: 60000
- assertVisible: "Email us"
- assertVisible: "Samuel Prak"
- assertVisible: "Eddy Monnot"
- tapOn:
    id: "about-changelog"
- extendedWaitUntil:
    visible: "Version 4.0"
    timeout: 60000
- assertVisible: "A fresh new design"
- assertVisible: "A faster calendar"
- assertVisible: "A truly native feel"
- stopApp
- launchApp
- extendedWaitUntil:
    visible: "Settings"
    timeout: 120000
- tapOn: "Settings"
- extendedWaitUntil:
    visible:
      id: "settings-about"
    timeout: 60000
- tapOn:
    id: "settings-about"
- extendedWaitUntil:
    visible: "Privacy policy"
    timeout: 60000
